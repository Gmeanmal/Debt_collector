# Changelog

All notable changes to this project are documented here. Format follows [Keep a Changelog](https://keepachangelog.com/), the project uses [Semantic Versioning](https://semver.org/), and commits follow [Conventional Commits](https://www.conventionalcommits.org/).

## [Unreleased]

### Fixed
- **I18N-1 locale homogenisation — single date helper, currency sweep, ESLint ban** (ref `planning/todo.md` W8, single `fix(ui)` commit):
  - **Shared `formatLondon` helper.** New `src/services/format/datetime.ts` exports `formatLondon(date: string | Date | null | undefined, kind: "date" | "datetime" | "time" = "datetime")`. All output in `Europe/London` via cached `Intl.DateTimeFormat` constructors (en-GB). Output spec frozen: `"date"` → `"5 Apr 2026"`, `"datetime"` → `"5 Apr 2026 · 14:32"` (middle-dot separator between date and time, 24h clock), `"time"` → `"14:32"`. `null`/`undefined`/`""` / invalid-date → `"—"`. Accepts both ISO strings and `Date` instances.
  - **47-file `toLocale*` migration.** Every `toLocaleDateString`, `toLocaleString`, `toLocaleTimeString` call across `src/` was replaced. `kind` was picked per call site: date-only renders → `"date"`, clock-only → `"time"`, rest → `"datetime"`. Edge cases preserved behaviour:
    - `PlanningCalendar.isoToday()` (computation, not display — used `en-CA` as an ISO date trick) rewritten with `Intl.DateTimeFormat.formatToParts()` to emit `YYYY-MM-DD` without touching `toLocale*`.
    - `PlanningCalendar.formatMonthLabel`, `PaymentChart.weekLabel`, `WeekRowButton.formatWeekLabel`, `TodayRoute` weekday label, `adminCronService.formatStartedAt` (includes seconds) kept a local `Intl.DateTimeFormat` because the required format isn't one of the three `formatLondon` kinds. These are still locale-safe, just not going through the helper.
    - `InvitationsListRoute`'s hand-rolled `Intl.DateTimeFormat(...).replace(",", " ·")` hack deleted — `formatLondon(iso, "datetime")` already emits the middle dot.
    - `date-time-picker.tsx::formatDisplay` now applies `Europe/London` via `formatLondon`, where it previously used browser-local time. Minor behaviour change, deliberate — the picker now matches the rest of the app.
    - `RollingReadonlyPanel.tsx`'s local `formatLondon` helper deleted in favour of the shared one.
  - **33-file currency sweep.** Raw `£${n.toFixed(2)}`, hand-rolled `Intl.NumberFormat("en-GB", { style: "currency" })`, and local `formatGbp` helpers all replaced with `formatGBP` from `services/format/currency`. Duplicate `formatGBP` in `services/dashboards/subDashboardFormat.ts` deleted and replaced with a re-export from the canonical module. Non-currency `.toFixed(2)` intentionally kept: percentage helpers (`fmtPct` in contracts), CSV numeric output (`weeklyCsv.ts`), payment input defaults, CSS-variable width (`TributeGauge`), and SimulatorPanel form compute values — all are computation or non-currency contexts.
  - **ESLint ban on raw `toLocale*`.** `client/eslint.config.js` `no-restricted-syntax` array gains three selectors — `MemberExpression[property.name='toLocaleDateString'|'toLocaleString'|'toLocaleTimeString']` — all with message `"Use formatLondon from @/services/format/datetime instead."`. `datetime.ts` itself uses `Intl.DateTimeFormat` and is unaffected. Verified firing by temporarily adding a call (removed before commit).
  - **FR string audit.** `grep -rn "jj/mm/aaaa|Bonjour|fr-FR" src/` returned zero matches pre-sweep and post-sweep. Nothing to remove — noted for the reviewer so the spec bullet can be closed.
  - **Gate green.** `pnpm tsc --noEmit` clean. `pnpm lint` clean.

### Changed
- **ROUTING-1 sub route standardisation under `/sub/*`** (ref `planning/todo.md` W8, single `refactor(client)` commit):
  - **Canonical moves.** `/today → /sub/today`, `/profile/kinks → /sub/profile/kinks`, `/profile/limits → /sub/profile/limits`, `/profile/aftercare → /sub/profile/aftercare`, `/profile/medical → /sub/profile/medical`. Each old path now renders a `<Navigate to="/sub/…" replace />` so bookmarks and prior session history land on the canonical URL (SPA-equivalent of the 308 the acceptance asked for — react-router cannot emit an HTTP redirect from a client-only shell; `<Navigate replace>` is the closest idiomatic match and preserves back-button behaviour).
  - **Not moved — deliberate.** `/debts/:slug` stays shared because goddess surfaces link into it (`ContractPreviewRoute`, `GoddessContractsRoute`, `SubContractsTab`, `LateContractsSection`, `PendingAdjustmentsPanel`, `LatePaymentList.deriveSubContractHref`) from non-sub roles. Moving it to `/sub/debts/:slug` would require either duplicating the detail route or breaking goddess/admin deep-links; both were rejected as out of scope for a standardisation slice. `/profile` (root identity card) also stays shared — `ProfileRoute` is used by goddess and admin too, no `RoleProtectedRoute` wrapper. Only the sub-only sub-pages (`kinks`, `limits`, `aftercare`, `medical`) actually moved.
  - **Nav updated.** `components/layout/navItems.ts::SUB_NAV` rewritten end-to-end: every `to: "/today"`, `"/profile/kinks"`, `"/profile/limits"`, `"/profile/aftercare"`, `"/profile/medical"` points at the new `/sub/…` canonical. `/sub/profile/inventory`, `/sub/journal`, `/sub/payments`, `/sub/payments/new`, `/sub/debts` were already prefixed; no change.
  - **Internal link sweep.** The one stale `<a href="/profile/aftercare">` inside `components/aftercare/AftercarePanel.tsx` (empty-state CTA) migrated to the new path. `src/api/aftercare.ts` and `src/api/medical.ts` left alone — those strings are server-side FastAPI routes (`/profile/aftercare`, `/profile/medical`), not React router paths, and are surfaced in the OpenAPI contract.
  - **`/sub/adjustments` already canonical.** KPI-2 had shipped an `/sub/approvals` alias against `/sub/adjustments`; a repo grep confirmed the alias is already gone and every surface (`SubSummaryGrid.tsx`, `PendingAdjustmentsRoute`) points at `/sub/adjustments`. Nothing to deprecate this slice — noted here so the next reviewer doesn't re-open the ticket.

### Fixed
- **LOGIN-1 login UX polish — contrast, boilerplate removal, icon-only theme toggle** (ref `planning/todo.md` W8, single `fix(auth)` commit):
  - **"Forgotten?" contrast.** Link class bumped from `text-base-text-subtle` (#847098 on dark — fails WCAG AA against the dark surface) to `text-base-text-muted` (#c4afd9) plus `underline underline-offset-2` so it's visibly a link before hover. Hover retains `text-pink-primary`.
  - **House-rules boilerplate removed.** The `"By signing in you accept the rules of the house"` paragraph under the submit button is deleted outright (planning default `removed` over linking to `/legal/house-rules` — no static page to back it up yet, and the run brief said to pick one and commit).
  - **Theme switcher → icon-only cycling toggle.** `components/layout/ThemeToggle.tsx` rewritten from a 3-radio `role="radiogroup"` to a single 9×9 circular button that cycles `system → light → dark → system`. Icon reflects the current preference (`Monitor / Sun / Moon` from `lucide-react`). `aria-label` announces both current and next states (e.g. `"Theme: System theme. Switch to Light theme."`) so screen readers pick up the cycling affordance; `title` mirrors just the current state for hover. Focus ring kept (`focus-visible:ring-2 ring-pink-ring`). `useTheme` hook untouched — `setPref` persist to `/me/preferences` still fires server-side.
  - **AuthLayout untouched.** The header-slot `<ThemeToggle />` at `absolute right-4 top-4` already sat outside the brand panel, so the switch from radios → single button took effect without any layout change. Auth pages now match the app header's placement.
  - **Email-only login + inline error banner already in place.** `LoginForm` previously shipped email-only validation and a `role=root` error banner returning `"Invalid email or password."` on the auth failure branch. Those paths were verified, not re-touched.

- **NOTIF-1 notifications popover polish — actor name, filter chips, mark-all-read, per-kind visuals** (ref `planning/todo.md` W8, single `feat(notif)` commit):
  - **Actor column.** Alembic `notif1_actor_user_id` adds `actor_user_id: UUID | None` FK to `notification`, `NotificationOut` gains `actor_display_name: str | None` and `actor_username: str | None`, and `NotificationController.list_for_user` bulk-resolves actors via a single `UserDao.get_many_by_ids` call (zero N+1). The popover renders `"{display_name} (@{username})"` when both are present, falls back to whichever exists, or omits the line entirely — raw UUIDs never hit the DOM for sub/goddess surfaces. Round-trip (`upgrade → downgrade → upgrade`) clean.
  - **Mark-all-read endpoint.** New `POST /me/notifications/read-all` → `204 No Content`. Full OpenAPI metadata (`tags=["notifications"]`, 401/500 responses). Backed by a new `NotificationDao.mark_all_read_for_user(user_id)` — one `UPDATE notification SET read_at=... WHERE user_id=:u AND read_at IS NULL`. No N+1.
  - **Dynamic bell `aria-label`.** Recomputed on every render as ``Notifications · ${unreadCount} unread`` — kept stable (never switches to `"No unread"`) so screen readers read the counter consistently regardless of state.
  - **Filter chips.** New `components/layout/NotificationFilterChips.tsx` renders `All / Validations / Late / Contract events` at the top of the popover. `services/notifications/notificationKinds.ts::FILTER_KINDS` maps chips to `NotificationType` sets:
    - `Validations` → `payment_pending`, `payment_validated`, `payment_rejected`.
    - `Late` → `rolling_late`, `contract_late_penalty`, `contract_surprise_penalty`.
    - `Contract events` → all 14 `contract_*` enum values.
    - `Kink updates` — **hidden via `HIDDEN_CHIPS`** because no current enum value covers kink changes (spec called for it but the `NotificationType` enum has no match today; a future slice can un-hide once the enum grows). Rendering an empty chip was rejected as noisier than hiding.
    - Chips carry unread-in-category counts in the label (e.g. `Validations · 3`). Buttons use `aria-pressed` with a visible focus ring.
  - **Mark all as read button** in the popover top-right. Disabled when `unreadCount === 0`. On click fires `markAllNotificationsReadApi` and invalidates the notifications query key. Pending state disables + shows a spinner.
  - **Per-kind visual differentiation.** New `components/layout/NotificationItem.tsx` resolves icon + accent per kind via helpers in `notificationKinds.ts`:
    - Icon: `lucide-react` — `BadgeCheck` for validation, `Clock` for late, `Heart` for kinks, `FileText` for contracts, `Bell` as fallback.
    - Colour accent: 2px left border + muted tint derived from existing tokens (`text-status-warning` / `text-status-success` / `text-pink-primary` / `text-base-text`). Tailwind utilities only; no inline hex.
    - Unread rows carry an extra `bg-base-surface-raised` tint plus a pink dot indicator.
  - **Component split.** `NotificationBell.tsx` stays under the 300-line cap; filter chips + the item row split into their own files (`NotificationItem.tsx`, `NotificationFilterChips.tsx`) so the orchestrator stays readable.

### Added
- **ADMIN-1 admin console polish — status pills, CSV export, sortable columns, impersonate confirm, delete guards** (ref `planning/todo.md` W8, single `feat(admin)` commit):
  - **Status pills.** New `components/admin/StatusPill.tsx` maps known status strings to semantic tokens via `components/admin/statusUtils.ts::isStatusColumn` (split to keep `react-refresh/only-export-components` happy): `active → bg-status-success/15 text-status-success`, `pending_entry_tribute → warning`, `blacklisted → danger`, `pending_validation → info`, `deleted → muted`; unknown values fall back to a neutral muted pill. `AdminTable` cell renderer checks `isStatusColumn(key)` before writing raw text.
  - **Sortable columns.** Every column in `services/admin/entitySchemas.ts` gained a `sortable: boolean` flag (action-only cells set to `false`). Click cycles unsorted → asc → desc → unsorted; arrow (↑/↓) only shown on the active column to avoid clutter. Client-side sort via `Array.prototype.toSorted`, handles string/number/date/null (nulls last). Runs after the existing role / status filters.
  - **Currency formatter.** New `services/format/currency.ts::formatGBP(amount)` wraps `new Intl.NumberFormat("en-GB", { style: "currency", currency: "GBP" })`. Accepts `number | string | null`; Decimal strings from FastAPI coerce via `Number()`; null / NaN → `"—"`. `entitySchemas.ts` columns gain a `format: "currency"` flag on `DebtContract.principal / minimum_payment / outstanding`, `PaymentDeclaration.amount`, `RollingTribute.amount`.
  - **Impersonate confirm modal.** `components/admin/ImpersonateConfirmModal.tsx` (≤100 lines) reuses the shared `Modal`. Body copy verbatim: `"You are about to impersonate {displayName}. All your actions will be logged to the audit trail. Continue?"`. Primary `"Impersonate"` (accent), secondary `"Cancel"`. Click on the row action now sets a `target` state; the existing impersonate mutation only fires on confirm.
  - **Delete protection.** `router.delete` (`server/routers/admin.py` generic CRUD factory) guards two cases BEFORE `session.delete(instance)`:
    - `model is User` AND `instance.role == admin` → counts remaining admins; if `≤ 1` → `409 Conflict("Cannot delete the last admin.")`.
    - `model is User` → counts `DebtContract` with `status IN (active, pending_sub_countersign, pending_goddess_countersign)` scoped to the row; any > 0 → `409 Conflict(f"User has {n} active contract(s). Close them first.")`.
    - `Conflict` exception added to `core/exceptions.py` following the existing `BadRequest / NotFound / Forbidden` pattern. Explicit `responses={409: {...}}` on every `DELETE` route.
    - Client wraps the existing DELETE in a `ConfirmActionModal`. 409 maps to the server's `detail` message via toast; the modal stays OPEN on 409 so the admin can cancel cleanly (only `setDeleteTarget(null)` on success).
  - **CSV export per table.** New `GET /admin/{entity}.csv` streamed via `StreamingResponse(media_type="text/csv")` with `Content-Disposition: attachment; filename="{entity}-{YYYY-MM-DD}.csv"`. Row batches of 500 via `session.stream(select(Model))`, `csv.writer` against an `io.StringIO()` flushed per batch. Password columns (`password_hash`) are stripped for `User` / `Goddess`. Full OpenAPI metadata per route. Client-side `adminApi.adminExportCsv(entity)` fetches the blob through `fetchWithAuth`, triggers a programmatic download via `URL.createObjectURL` + `<a>` click + revoke, toasts `"Export ready"` / `"Export failed"`.
  - **Sidebar accent token.** `routes/admin/AdminRoute.tsx` active nav item swapped from `bg-violet-primary text-violet-foreground` to `bg-accent text-accent-foreground` (rose token already declared in `tokens.css` as `--accent: var(--color-pink-primary)`). Heading colour `text-violet-primary` → `text-accent`. No new variable added — the existing token routes to the rose primary.
  - **"+ New" hidden on Debt contracts.** `entitySchemas.ts` gained `canCreate: boolean`; `AdminTable` hides the button when false. Set `canCreate=false` on `DebtContract` with a one-line comment: `// Debt contracts are goddess-authored via /goddess/contracts; admin console is read/patch only.`
  - **Component refactor.** `AdminTable.tsx` split into `AdminTable.tsx` (orchestrator) + `AdminTableBody.tsx` + `AdminTableToolbar.tsx` so no file exceeds the 300-line cap. `DeleteConfirmModal.tsx` extracted for the same reason.

- **CRON-1 admin cron dry-run, run history and audit trail** (ref `planning/todo.md` W7, single `feat(admin)` commit):
  - **New `cron_run` table.** Alembic `4e3b560cf217` creates `cron_run(id UUID PK, started_at, finished_at nullable, dry_run bool, summary_json JSONB default '{}', errors JSONB default '[]', triggered_by_user_id UUID FK user.id ON DELETE SET NULL, duration_ms int nullable)` with `ix_cron_run_started_at DESC` for the history list. All JSONB defaults registered as `sa.text("'...'::jsonb")` — asyncpg round-trips a Python dict via `server_default` were rejected on first attempt. Round-trip (`upgrade → downgrade → upgrade`) clean.
  - **Dry-run savepoint.** `CronController.run_daily(dry_run, triggered_by_user_id)` now wraps the mutating block in `async with session.begin_nested() as sp:` and calls `await sp.rollback()` at the end when `dry_run=True` — counters are still computed accurately, side-effects (rolling tributes + contract period ticks) are discarded. The `CronRun` row itself sits OUTSIDE the savepoint so the dry-run history entry always persists. Per-sub errors are captured in a `[{sub_id?, phase, message}]` list rather than aborting the run; the summary captures `{subs, rolling, contracts, fees_applied, breach_candidates}`.
  - **Three replacement endpoints.** The old naked `POST /admin/cron/run-now` + its inline `CronRunOut` schema are deleted. New surface (all `tags=["admin-cron"]`, admin role required, full OpenAPI metadata):
    - `POST /admin/cron/dry-run` → `CronRunSummaryOut`. Fires a dry-run; no effect on app state except the `cron_run` history row.
    - `POST /admin/cron/apply` — body `{ last_dry_run_id: UUID }`. Server enforces `(a)` the referenced run exists, `(b)` was a dry-run, `(c)` started within the last 5 minutes, `(d)` was triggered by the same admin. Any violation → `409 Conflict` with a readable message. Returns `CronRunSummaryOut`.
    - `GET /admin/cron/runs?limit=50` — returns recent runs newest-first. Used by the history table.
  - **Audit entry per run.** Each run writes one `audit_event` of `kind="cron_run"` with `entity="cron_run"` and `entity_id=run_id`. The existing `@audit` decorator couldn't extract `entity_id` from the response body (the payload exposes `run_id`, not `id`), so the two mutating routes call `AdminActionDao.record()` manually with the explicit `entity_id`. The read-only GET has no audit.
  - **Rebuilt `/admin/cron` page.** Two-section layout:
    - **Top — "Run cron now".** Two-button dance: `Run dry-run` (primary) fires the dry-run mutation and stashes the result in local state with a capture timestamp. Results card below lists every `summary` counter (keys rendered dynamically as sentence case) + errors. `Confirm & apply` is enabled ONLY when (i) a dry-run result exists, (ii) the capture timestamp is within the 5-minute window, (iii) the dry-run had zero errors. Expired window shows a muted hint `"Dry-run expired, run it again."`. 409 on apply → server message toast, window still alive for a fresh dry-run. On success the dry-run result is cleared so the button returns to disabled — an admin who wants to run again must dry-run again.
    - **Bottom — "Run history".** Sortable-implicit (server-ordered) table of the last 50 runs: Started (`D Mon YYYY · HH:mm:ss` `en-GB`), Kind (`Dry-run` muted vs `Real` pink badge), Duration (`formatDuration ms → "1.2s" / "42ms"`), Errors (count; `text-status-danger` when > 0). Click-to-expand panel renders the full `summary` + errors list; single-row-open is fine.
  - **UUID rendering under admin role.** Error rows may reference a sub by UUID only (engine captures `sub_id` before user resolution); per the CLAUDE.md rule admin is the only role that may render raw UUIDs, so the expanded panel shows a 4-char prefix with the full UUID in a `title=` tooltip. No goddess_id or sub_id escapes to goddess/sub surfaces.
  - **Component split.** `routes/admin/AdminCronRoute.tsx` rebuilt; sub-components extracted to `components/admin/cron/DryRunResultCard.tsx`, `RunHistoryTable.tsx`, `RunHistoryExpandedRow.tsx` to keep each file under the 300-line cap. Service helpers (`formatDuration`, `wasDryRunTooOld`, `toSentenceLabel`, `formatStartedAt`) live in `services/admin/adminCronService.ts`. Dead `services/admin/cronApi.ts` deleted (referenced the retired `run-now` endpoint).

- **PENALTY-1 seeded default penalty rules, reward tiers and 4-step rule wizard** (ref `planning/todo.md` W7, single `feat(payments)` commit):
  - **Schema columns.** Alembic `259030fa7f44` adds three nullable columns to `penalty_rule`: `name: Text` (short slug, e.g. `late_7d_5pct_fee`), `fee_percent: Numeric(5,2)` with `CHECK (fee_percent BETWEEN 0 AND 100)`, and `min_days_late: Integer` (rolling-late threshold gate). All-nullable so existing user-created rules keep loading; no backfill needed. Round-trip (`upgrade → downgrade → upgrade`) clean.
  - **Seeder service.** New `services/penalty/defaults.py::seed_defaults_for_goddess(session, goddess_id)` is an idempotent (skip-if-name-matches) async helper that INSERTs 3 penalty rules + 4 reward tiers for a brand-new goddess. Seed spec — rules all `trigger=rolling_late`, `sub_id=None`, `active=True`:
    - `late_2d_notify` — action `notify_only`, `points_delta=0`, `min_days_late=2`, `cooldown_hours=48`.
    - `late_7d_5pct_fee` — action `apply_fee`, `points_delta=-3`, `fee_percent=Decimal("5.00")`, `min_days_late=7`, `cooldown_hours=168`. (Flat-% fee: the engine computes the GBP amount from the sub's minimum payment at apply time; no `fee_amount` on the rule.)
    - `late_14d_breach_warn` — action `notify_only`, `points_delta=-10`, `min_days_late=14`, `cooldown_hours=336`.
    - Tiers: `bronze` (cost 30 · "1 month perfect"), `silver` (cost 90 · "3 months perfect"), `gold` (cost 180 · "6 months perfect"), `crown` (cost 365 · "12 months perfect"). Caller owns the commit boundary.
  - **Wired into every goddess-creation path.** `seeds/bootstrap.py` (dev seed script that creates the locked admin + goddess per the memory) now calls the seeder right after the goddess row flushes. `routers/admin.py` admin-console create-goddess path calls the same helper behind an `if model is Goddess:` guard — the admin router uses a reflection-based generic CRUD factory with no dedicated controller, so this is the first and only layer where the call is reachable (documented deviation; a future ADMIN-1 slice can move it into a proper controller).
  - **Engine picks the right threshold.** `daos/penalty_rule_dao.py::find_matching_rule` now takes `days_late: int | None`: for `rolling_late` rules it keeps only `min_days_late ≤ days_late` (or NULL, to preserve legacy behaviour) and picks the highest threshold first — so a sub who is 10 days late fires the 7-day rule, not the 2-day one. Per-sub override still wins before global fallback. `services/penalty/engine.py::apply_penalty` plumbs the new parameter through (keyword, default None so unrelated callers don't break).
  - **Controller validation guard.** `PenaltyRuleController.create` and `.update` reject `fee_percent` outside `[0, 100]` with `Validation("fee_percent must be between 0 and 100", fee_percent=...)` — belt-and-braces over the DB CHECK.
  - **4-step wizard frontend.** `components/penaltyRules/PenaltyRuleForm.tsx` (289 lines) rebuilt as a 4-step wizard with grammar hints in the header: `1/4 · Trigger`, `2/4 · Condition`, `3/4 · Action`, `4/4 · Amount`. Step components extracted to `components/penaltyRules/wizard/` (`Step1Trigger.tsx` 71 lines, `Step2Condition.tsx` 86 lines, `Step3Action.tsx` 72 lines, `Step4Amount.tsx` 187 lines, shared `types.ts`) to keep every file under the 300-line cap. Zod step-boundary validation; no form library.
    - **Step 2** shows `min_days_late` input only when trigger is `rolling_late` with live-preview sentence `"Applies once a sub is N days late."`. Per-sub override uses a `SearchableSelect<SubOption>` labelled `Only for this sub (optional)` — resolves LOOSE-2 via DROPDOWN-1. UUID rule preserved — the picker shows `display_name` + muted `@username`.
    - **Step 4** amount widgets gated by action: `notify_only` → just `points_delta`, `apply_points` → required `points_delta`, `apply_fee` → segmented control toggling between flat `fee_amount` (GBP) and `fee_percent` (0..100) with Decimal inputs. `name` (slug hint) / `cooldown_hours` / `active` in a collapsible Advanced `<details>` at the bottom; no Radix dependency for a one-off disclosure. Submit body omits stale fields per trigger/action so `fee_percent` never leaks into a `notify_only` rule.
  - **Edit uses the same wizard** — pre-filled at step 1. No separate edit form; seeded defaults editable like any user-created rule (the spec's "no read-only seed" criterion). Existing disable-toggle keeps PATCH `active=false` semantics.
  - **List upgrades.** `PenaltyRuleList.tsx` (252 lines): new `name` column (shows `"—"` when null), humanised `rolling_late` trigger rendered as `"Rolling late · ≥ N days"`, unified fee cell showing `"£12.50"` or `"5 %"` depending on which of `fee_amount` / `fee_percent` is populated, per-row delete via the shared `ConfirmActionModal` (RITUALS-1 pattern), per-sub override row displays `display_name @username` (never UUIDs).
  - **Small schema fix shipped along the way.** `PenaltyRuleInSchema` (client) dropped an over-tight `points_delta ≤ 0` constraint that was blocking legitimate positive adjustments on `apply_fee` / `notify_only` rules (e.g. reward-leaning tuning by the goddess). Server side already accepts any int.

- **RITUALS-1 goddess rituals UI with cross-sub aggregate list and assign modal** (ref `planning/todo.md` W7, single `feat(rituals)` commit):
  - **Schema column.** Alembic `eb4db276ecf4` adds `requires_proof: bool NOT NULL DEFAULT FALSE` (server_default `sa.false()` for safe backfill) to `ritual`. Propagated through `models/ritual.py`, `schemas/rituals.py` (`RitualCreateIn`, `RitualUpdateIn`, `RitualOut`), and persisted in `RitualController.create_ritual` + `update_ritual`. When the flag is set, `complete_occurrence` + `submit_occurrence` raise `BadRequest("This ritual requires photo/evidence to mark complete.")` if the submitted payload has no `evidence_r2_key` — defence-in-depth, not only a UI hint. Round-trip (`upgrade → downgrade → upgrade`) clean.
  - **New aggregate endpoint `GET /goddess/rituals`.** `tags=["rituals"]`, `response_model=list[RitualWithSubOut]`, full OpenAPI metadata (summary, description, 401/403 responses). New `RitualWithSubOut(RitualOut)` carries `sub_display_name` + `sub_username` embedded — zero N+1 via single JOIN in new `RitualDao.list_for_goddess_with_sub_names(goddess_id)`. Returned newest-first, INCLUDES paused rituals (frontend owns the filter). `User.display_name` isn't a DB column — we derive `first_name + last_name` with `username` fallback, mirroring the `_display_name` helper in `goddess_views_controller.py`.
  - **New `/goddess/rituals` page.** `routes/goddess/GoddessRitualsRoute.tsx` (248 lines) — groups rituals by sub (section header = `sub_display_name` + muted `@sub_username`, never the raw UUID). Each row: title, schedule-preview sentence, description (muted, 2-line clamp), `Paused` / `Requires proof` badges, `points_on_complete` / `points_on_miss`. Per-row actions: Pause/Resume (PATCH `paused`), Delete (reuses the shared `ConfirmActionModal`). Empty state copy: "No rituals assigned yet. Use the form above to assign one." `+ Assign ritual` button in the header opens the modal. Added `goddess.rituals()` query key + nav entry under `components/layout/navItems.ts::GODDESS_NAV` + `router.tsx` registration behind `ProtectedRoute + RoleProtectedRoute(goddess)`.
  - **Assign modal.** `components/rituals/AssignRitualModal.tsx` (289 lines) — `SearchableSelect` for sub (display label `display_name` + muted `@username`, UUIDs never rendered), `<select>` for frequency (daily / weekly / custom), 7 day-chip toggles with bitwise encoding (`Mon=1 .. Sun=64`) when custom, `<input type="time">` for `deadline_time` (default `23:59`), `requires_proof` checkbox with hint "Sub must attach a photo to mark complete", points inputs with sensible defaults. Live preview sentence under the form updates as fields change so the goddess reads the rendered schedule before submit. Invalidates `queryKeys.goddess.rituals()` on success. Validation errors map to `toast.error`; 422 keeps the modal open.
  - **Schedule preview helper.** `services/rituals/schedulePreview.ts::formatRitualSchedule({ frequency, custom_days_bitmask, deadline_time })` — daily → `"Every day at HH:MM"`, weekly → `"Every Monday at HH:MM"` (single-anchor fallback matches current weekly cron semantics), custom → `"Every Mon·Wed·Fri at HH:MM"` (short names, `·` separator, Mon→Sun order). Handles `deadline_time` as `HH:MM` or `HH:MM:SS`, falls back to `23:59` when null. Consumed by both the list row and the modal's live preview — single source of truth.
  - **Cron ingest.** `services/cron/rituals.py::seed_occurrences_for_today` already reads non-paused rituals via `list_active_for_date` — no plumbing needed. The acceptance criterion "Cron job ingests new rituals → fills sub Today page automatically" is satisfied the next daily run after creation; confirmed by reading the service. The one-shot "Run cron now" admin button fires the same code path, so a goddess can verify assignment by having admin run cron, but no new cron wiring ships in this slice (CRON-1 will rebuild that surface).
  - **Typed client layer.** `api/rituals.ts` (70 lines) — `listGoddessRituals`, `createRitualForSub`, `updateRitual`, `deleteRitual`, `pauseRitual`, `resumeRitual` via `openapi-fetch`. Re-exports generated types. One-way import rule preserved — `routes → components → services → api`.

- **SUBDETAIL-1 sub detail tabs grouped by theme with quick-message action** (ref `planning/todo.md` W6, single `feat(goddess)` commit):
  - **Tab layout.** `routes/goddess/subs/SubManageRoute.tsx` now renders 13 tabs grouped into 4 visually-separated bands (`Money` · `Profile` · `D/s` · `Things`) via lightweight inline group labels + `h-4 w-px bg-base-border/60` vertical separators inside the existing `TabsList`. Group labels are plain `<span>`s (not `TabsTrigger`s) — non-interactive, non-focusable, `uppercase tracking-[0.12em] text-base-text-muted`. Tab state preserved (still driven by Radix tabs); default value stays `overview`.
    - **Money:** `Overview`, `Rolling`, `Contracts`, `Late`. **Profile:** `Profile`, `Kinks`, `Limits`, `Aftercare`. **D/s:** `Rituals`, `Journal`, `Merits`. **Things:** `Inventory`, `Devices`.
  - **8 new tab content files.** `SubKinksTab.tsx` (163 lines), `SubLimitsTab.tsx` (174 lines), `SubAftercareTab.tsx` (13-line wrapper around the existing `SubAftercarePanel`), `SubRitualsTab.tsx` (141 lines), `SubJournalTab.tsx` (143 lines), `SubMeritsTab.tsx` (108 lines), `SubInventoryTab.tsx` (64 lines), `SubDevicesTab.tsx` (58 lines). All read-only for the goddess: kinks / limits / rituals render as structured `<dl>` + chip lists; journal reuses the pre-JOURNAL-1 `JournalEntryCard` + `GoddessCommentForm` + mark-read button; merits renders a timeline of events with signed delta badges; inventory / devices reuse the shared `InventoryGrid`.
  - **Inventory ↔ Devices split.** Both tabs share one `goddessSubToysKey(subId)` query; client-side partition by `toy_item.category === "cage"` (devices) vs all other categories (inventory). No extra server resource — this is a UX split only.
  - **Bulk read-surfaces (server).** `GET /goddess/subs/{sub_id}/merit-events` (new `MeritsController.list_events_for_sub` → new `MeritEventDao.list_for_sub` with ownership check inside the controller, 404-on-wrong-goddess). `GET /goddess/subs/{sub_id}/photos/top` (new `SubPhotoDao.top_approved_for_sub` — returns the single most-recent approved photo for header avatar fallback, nullable). Wired into `SubProfileCard` with `<img loading="lazy" width=64 height=64>` when available. Layered surfaces all have full OpenAPI metadata.
  - **Send-message quick action.** New `components/goddess/SendMessageQuickAction.tsx` (132 lines) renders a `MessageCircle`-iconed button next to the `SubProfileCard` in the detail header. Opens a `Modal` with a `<textarea>` (5–500 chars, inline `text-base-text-muted` character counter, trimmed length guard, `aria-describedby` wired to both the label and the counter). On submit → `POST /goddess/subs/{username}/message` (new router `goddess_subs_message.py` → `notify_controller.send_direct_message`) which enqueues a `goddess_message` notification for the sub. New Alembic `subdetail1_goddess_message_notif` adds the `goddess_message` value to the `notificationtype` Postgres enum (downgrade is a documented no-op — PG has no `DROP VALUE`). Success fires `toast.success("Message sent")`, modal closes; 404 / 422 map to distinct toasts. Query invalidation touches `queryKeys.notifications.all()` so the sub's unread badge stays fresh if both sides are open.
  - **New shared client layer.** `api/goddessSubDetail.ts` + `services/goddessSubDetail/goddessSubDetailApi.ts` expose the read-only per-sub fetchers (`getSubKinkMatrixForGoddess`, `getSubLimitsForGoddess`, `getSubTriggersForGoddess`, `getSubSafewordForGoddess`) and the write-only `sendGoddessMessageToSub`. Consumers include only those, never the sub-side mutation wrappers. New query keys: `subPhotos.topApproved(subId)`, `subKinks.forSub(subId)`, `subLimits.forSub(subId)` / `triggersForSub(subId)` / `safewordForSub(subId)`, `rituals.forSub(subId)`, `meritEvents.forSub(subId)` — scoped under per-resource namespaces so invalidation from the matching sub-facing route can target both the sub and goddess caches when needed.
  - **Type sync.** `pnpm sync-types` regenerates `src/types/api.generated.ts` against the running server so the new endpoints (`/goddess/subs/{sub_id}/merit-events`, `/goddess/subs/{sub_id}/photos/top`, `/goddess/subs/{username}/message`) reach the typed `openapi-fetch` client. Build gate green (ruff + pyright + tsc + ESLint).

- **AFTERCARE-1 intensity slider + goddess-read signal** (ref `planning/todo.md` W5, single `feat(profile)` commit):
  - **Schema.** Alembic `07d331e36298` adds `intensity: int NOT NULL DEFAULT 3` with `CheckConstraint(1 <= intensity <= 5)` and `read_by_goddess_at: datetime | None` to `sub_aftercare`. Migration is defensive — detects whether the table already exists (one prior migration had been stamped without applying on some environments) and adapts accordingly. Downgrade is symmetric. Round-trip (`upgrade → downgrade -1 → upgrade`) clean.
  - **Sub form.** `AftercareEditForm.tsx` now ships an `IntensitySlider` sub-component with `<input type="range" min=1 max=5 step=1>`, live-updating label, `aria-describedby`-linked help text (`1 = gentle · 5 = intense`). Keyboard-navigable by default. When `read_by_goddess_at` is set, a muted `Goddess read · <rel>` pill renders below the save button.
  - **Goddess side.** New `GET /goddess/subs/{username}/aftercare` + `POST /goddess/subs/{username}/aftercare/read` (204). The POST is idempotent — the first call stamps the timestamp via `mark_read_if_unset`, subsequent calls noop. Ownership check raises `NotFound` (not `Forbidden`) when the sub does not belong to the caller so existence never leaks. Wired into the Overview tab of the goddess sub detail route via a new `SubAftercarePanel.tsx`: renders the full aftercare card, an intensity badge, a "Mark as read" button, and a `Goddess last read · <rel>` subtext when present.

- **JOURNAL-1 attachments, private toggle and goddess-read signal** (ref `planning/todo.md` W5, single `feat(journal)` commit):
  - **New columns on `journal_entry`.** Alembic `74c88e3644c0` adds `attachment_key: varchar | null`, `attachment_mime: varchar | null`, and `is_private: boolean NOT NULL DEFAULT false`. The legacy `photo_r2_key` column stays alive for backwards-compat — `attachment_key` is the primary surface, `photo_r2_key` still receives a copy of the key when the attachment mime is an image. A TODO in the model points to a future consolidation slice.
  - **Multipart upload on `POST /sub/journal`.** Form body: `body`, `mood`, `is_private`, optional `attachment: UploadFile`. Whitelisted mimes: `image/jpeg|png|webp`, `audio/mpeg|ogg|webm`. 10 MB hard cap (400 on breach). New MinIO bucket `journal-attachments` (`docker-compose.yml` `minio-init` entrypoint now creates it idempotently). `Settings.s3_bucket_journal_attachments` added.
  - **Private-entry filtering.** Both goddess-side list endpoints (`daos/journal_dao.py::list_for_goddess` + `list_for_sub_as_goddess`) filter `is_private = False` at the SQL level — private entries never reach the controller for goddess callers. Sub's own list (`GET /sub/journal`) still returns every entry. No journal-created notification exists today, so no filter needed there.
  - **Explicit mark-read endpoint.** `POST /goddess/subs/{username}/journal/{entry_id}/read` (204) — delegates to `journal_controller.mark_read` which calls `journal_dao.mark_read_if_unset(entry_id)`. Idempotent: the first call stamps `read_by_goddess_at`, subsequent calls noop. 404 when the entry belongs to another goddess (never 403 — don't leak existence). The pre-existing bulk auto-mark on `GET /goddess/subs/{sub_id}/journal` keeps its behaviour; this endpoint is for explicit UI actions ("Mark as read" button on unread entries).
  - **Presigned-URL surfacing.** `JournalEntryOut` gains `attachment_key`, `attachment_mime`, `attachment_presigned_url`, `is_private`. Controller's `_build_out` presigns URLs per row and strips the raw key from the response (keys are internal — only the presigned URL reaches the client). Sub + goddess surfaces render `<img loading="lazy">` for image mimes and `<audio controls>` for audio mimes.
  - **Sub-side "Goddess read" signal.** `JournalEntryCard.tsx` renders a muted `Goddess read · <rel>` pill whenever `read_by_goddess_at` is non-null, using the existing relative-time helper.
  - **Sub form extras.** `JournalEntryForm.tsx` gains a `<input type="file" accept="…"/>` with a client-side 10 MB guard + a `Private (only me)` checkbox with a muted hint `Private entries stay hidden from your Goddess.`. Form is always multipart on submit. Goddess reader route shows a "Mark as read" button on unread entries (query invalidation on success).

### Fixed
- **PROFILE-1 profile header polish + inline change-request panel** (ref `planning/todo.md` W5, single `fix(profile)` commit):
  - **Header layout.** `/profile` now renders `display_name` on line 1 (`font-display` pink-primary) and the derived `@handle` on line 2 (muted, small). Duplicate `display_name` line removed. Note: `UserOut` does not yet expose `username` — handle is derived from the email prefix via `deriveHandle(email)` until a future slice adds `UserOut.username`.
  - **Payment handle copy.** `"Visible only to you."` → `"Visible to you and your Goddess."` on the payment-handle card so the visibility contract matches reality (the goddess reads it from the validation UI).
  - **Avatar save toast.** The existing `onSuccess` callback on the avatar upload now fires `toast.success("Avatar saved")`.
  - **Inline change-request panel.** `ChangeRequestDialog` (Radix dialog) replaced with a collapsible inline `<section role="region" aria-labelledby>` inside `ProfileRoute.tsx`, toggled by a `useState<boolean>` + a button carrying `aria-expanded` + `aria-controls`. Collapsed by default; auto-collapses on cancel or successful submission. Form JSX extracted into `components/profile/ChangeRequestPanel.tsx`. The legacy dialog file is deleted — no backwards-compat shim.

### Added
- **GENDER-1 72-entry gender taxonomy with searchable picker** (ref `planning/todo.md` W5, single `feat(profile)` commit):
  - **New `gender_taxonomy` table.** `models/gender_taxonomy.py::GenderTaxonomy(id UUID PK, slug VARCHAR UNIQUE, label VARCHAR, description VARCHAR nullable, sort_order INT, created_at TIMESTAMP)`. Alembic migration `b06e2c961616` seeds 72 deterministic rows via `uuid5(NAMESPACE_URL, slug)` so upgrade → downgrade → upgrade round-trips produce stable primary keys — no drifting FK references on rollback. Seed list drawn from Facebook's 2014 identity list + standard queer-inclusive extensions (cis/trans/non-binary variants, agender, bigender, genderfluid, genderqueer, demi-boy/girl/non-binary, two-spirit, hijra, muxe, xenogender, `prefer_not_to_say`, etc.). Sort order is alphabetical by label (1..72).
  - **FK swap on `sub_profile`.** Old 4-value `Gender` StrEnum (`male / female / non_binary / other`) dropped entirely; `sub_profile.gender` column replaced with `gender_id: UUID | None` referencing `gender_taxonomy.id`. Data migration maps `male → cis_man`, `female → cis_woman`, `non_binary → non_binary`, `other → NULL` via `op.execute` before the enum type is dropped. `downgrade()` recreates the enum, reverse-maps the three mapped slugs back, and NULL-drops everything else.
  - **Reference endpoint.** `GET /reference/genders` (`tags=["reference"]`, auth: any authenticated role) returns `list[GenderTaxonomyOut]` sorted by `sort_order`. Clean layered surface — new `routers/reference.py` delegates to `controllers/reference_controller.py::list_genders` which calls `daos/gender_taxonomy_dao.py::list_all`. Full OpenAPI metadata (summary, description, response_model, status_code, responses={}).
  - **Schema propagation.** `UserOut` gains `gender_taxonomy: GenderTaxonomyOut | None` (resolved and embedded controller-side in `_user_out` helpers across `auth.py`, `me_preferences.py`, `goddess_profile.py` — explicit join, no N+1). `ProfileUpdate` gains `gender_id: UUID | None`; `SignupRequest` gains `gender_id: UUID | None` (the invite signup form still renders a free-text `gender` input — migration to the picker is intentionally deferred, out of this slice). The pre-existing free-text `UserOut.gender` column on the `user` table is untouched.
  - **Frontend picker.** `IdentityFieldsCard.tsx` swaps the old free-text gender `Input` for `SearchableSelect<GenderTaxonomyOut>` (from W1 DROPDOWN-1), with `aria-label="Gender"` and a helper `<p class="text-xs text-base-text-muted min-h-[1rem]">` showing the currently-selected entry's `description`. New `api/reference.ts::listGenderTaxonomyApi` + `hooks/useGenderTaxonomy.ts` (`staleTime: 1h` — the taxonomy is static reference data). `queryKeys.reference.genders()` added. PATCH body now carries `gender_id` (UUID) rather than a label string; the pre-existing free-text `gender` field is preserved in the PATCH body so the form does not accidentally null it.

### Fixed
- **LIMITS-1 safeword required to activate account** (ref `planning/todo.md` W5, single `fix(profile)` commit):
  - **Server state-machine guard.** `PaymentController._promote_sub` now fetches the sub's `SubSafeword` via `SubSafewordDao.get_for_sub` before flipping `UserStatus.active`. Missing or blank-word rows raise `Validation("A safeword must be set before activating the account.", sub_id=...)` from `core.exceptions` — never reaches the `sub.status = UserStatus.active` line. Covers both entry-tribute promotion paths (`declare_as_sub` + goddess-side validation).
  - **Placeholder sentinel rejection.** New `server/utils/placeholder_guard.py::reject_if_placeholder(value, field)` rejects (case-insensitive, trimmed) `jane doe`, `john doe`, `+44 7700 900000`, `+447700900000`, `test`, `placeholder` with `Validation("This value looks like a placeholder, please enter a real one.", field=...)`. Wired into `SubSafewordController.upsert_self` (`word` / `emergency_contact_name` / `emergency_contact_phone`) and `LimitsController.create_own_limit` (`label`). None / empty values pass through — required-field emptiness is still Pydantic's job.
  - **Frontend safeword gating.** `LimitsRoute.tsx` reads `useAuth().user` + the safeword query; when `status === "pending_entry_tribute"` and the word is empty, an amber banner reads "Set a safeword below before paying your entry tribute." above the safeword form. `PorchLayout.tsx` swaps the "Declare entry tribute" CTA for the same hint when the safeword is missing so the sub cannot reach the declare form. `PaymentFormRoute.tsx` disables submit + shows the same hint when `category === "entry"` and the safeword is missing — defence-in-depth if a sub navigates to `/sub/payments/new?kind=entry_tribute` directly.
  - **"What your goddess sees" preview.** New `components/limits/GoddessPreviewPanel.tsx` renders a read-only `<dl>` summary of safeword / emergency contact / limits / triggers exactly as the goddess-side panel shows them. Plain DOM, no UUIDs, no mutations, no network beyond the already-fetched queries. Disabled fields / muted tokens so the read-only contract is visually obvious.

### Added
- **KINKS-1 sub kinks UX hardening with consent acknowledgement** (ref `planning/todo.md` W5, single `feat(profile)` commit):
  - **New `prefer_not_to_say` rating.** Postgres ENUM `kinkrating` gained a seventh value via Alembic migration (`autocommit_block` for the non-txn `ALTER TYPE ADD VALUE IF NOT EXISTS`; downgrade is a documented no-op since PG has no `DROP VALUE`). `models/sub_kink_rating.py::KinkRating` and `services/kinks/kinksApi.ts::KinkRatingSchema` extended to match. The new value is distinct from the default `not_set` and never triggers `needs_confirmation`.
  - **Confirmation scope narrowed.** `_CONFIRMATION_RATINGS` in `controllers/kinks_controller.py` dropped from `{curious, loves, fetish_need}` to `{loves, fetish_need}`, matching the spec's "before allowing `+` or `++`" rule. Schema docstrings on both `SubKinkRatingOut.needs_confirmation` and `KinkItemOut.needs_confirmation` updated so Swagger reflects the narrowed contract.
  - **Client-side consent-ack interception.** New `components/kinks/ConsentAckModal.tsx` (52 lines, reuses the shared `Modal` primitive — focus trap / Esc / overlay inherited). `KinkRow.tsx` now intercepts a `+` / `++` click on any safety-flagged item BEFORE firing the mutation: the modal surfaces `"This activity can cause permanent harm. By rating this as <+|++>, you confirm that you understand the risks and give informed consent."`, a `"I understand and consent"` checkbox gates the Confirm button, Cancel aborts. The server-side `needs_confirmation` response stays as a data-consistency signal; the UI no longer renders anything reactive off it. Guard uses direct equality (`rating === "loves" || rating === "fetish_need"`) — no type casts.
  - **Accordion by category, collapsed by default.** `KinkMatrix.tsx` (143 lines) replaced the flat list with a collapsible accordion. Each category header is a plain `<button>` with `aria-expanded` + `aria-controls` + `id="category-header-{id}"`; the panel is `role="region" aria-labelledby="category-header-{id}"` and is conditionally mounted (not `hidden`) so collapsed categories shrink the page. Header shows `N items · M rated` (with `N_visible/N_total items · M rated` when the filter is on — M always counts absolute rated items, not filtered).
  - **"Show only unrated" filter.** Top-level checkbox in `KinksRoute.tsx` (148 lines). When on, categories render only `rating === "not_set"` items; categories that end up empty under the filter are hidden entirely.
  - **Per-row save indicator.** `SaveIndicator` (inside `KinkRow.tsx`, 117 lines) owns its own self-ticking 1 s interval, started only when `lastSavedAt` is set and within the 6 s window. No route-level `setInterval`, no `now` prop drilling — untouched rows never re-render. Shows `"Saving…"` (with spinner) while the mutation is in-flight, then `"Saved · Ns ago"` until the window closes.
  - **Tooltips on rating buttons.** Native `title=` attribute on each `RatingPicker.tsx` button (107 lines): `X → "Hard limit"`, `– → "Dislike"`, `? → "Curious"`, `+ → "Like"`, `++ → "Crave"`. "Prefer not to say" button sits after a vertical divider with no tooltip (label is self-explanatory).

### Added
- **LATE-1 widened `/goddess/late` with rolling + contracts sections** (ref `planning/todo.md` W4, single `feat(goddess)` commit):
  - **New read-model endpoint.** `GET /goddess/contracts/late` → `list[LateContractItem]` (200, goddess role). Sibling to the existing `GET /goddess/subs/late`. Returns active contracts with `signed_at` set whose current period has no `payment_applied` event and whose `period_start` is in the past. Predicate mirrors `DashboardController._contracts_late_map` exactly (UTC-naive date arithmetic, `days_late` capped at `period_length_days(payment_frequency)`) so the adjacent dashboard late-card and the widened `/goddess/late` page can never disagree. Bulk data loads: one `DebtContractDao.list_active_for_goddess`, one `DebtEventDao.paid_period_indices_for_contracts`, one new grouped `DebtEventDao.last_applied_per_contract(contract_ids)` that does `MAX(created_at) GROUP BY contract_id WHERE event_type = payment_applied`. Zero N+1.
  - **Schema.** `schemas/goddess_views.py::LateContractItem` carries `contract_id`, `slug`, `sub_id`, `sub_display_name`, `sub_username`, `days_late`, `overdue_amount` (`minimum_payment`), `last_payment_at`. `sub_id` / `contract_id` are cache-key only — UUID rule keeps them out of DOM text. `sub_username` surfaces so the client can link to `/goddess/subs/{username}` without a second roundtrip.
  - **Widened `/goddess/late` page.** `LateSubsRoute.tsx` renamed to `LateRoute.tsx` (export: `LateRoute`, 89 lines), page title now reads "Late" with two `<Separator>`-divided sections. Each section owns its own `useQuery`, its own sortable table, its own link target (rolling → `/goddess/subs/{username}`, contracts → `/debts/{slug}` on an optional 5th "Contract · View" column when slug is non-null). Sortable columns on both: Name, Days late, Overdue (GBP), Last payment — the `SortHeader` pattern from pre-LATE-1 rolling was duplicated into each section (marked for consolidation when a third sortable table lands).
  - **Shared colour helper.** `services/goddess/lateColour.ts::daysLateClass(days: number)` returns the Tailwind token: `"text-status-danger"` for `days ≥ 7`, `"text-status-warning"` for `3 ≤ days ≤ 6`, `"text-base-text"` otherwise. Consumed by both `LateRollingSection` and `LateContractsSection` so the severity ramp is identical.
  - **API wrapper.** `services/goddess/lateContractsApi.ts::getLateContractsApi()` via `openapi-fetch` (mirrors `lateSubsApi.ts` pattern). `queryKeys.goddess.lateContracts()` keyed sibling to `lateSubs()`.
  - **Scope trim (intentional).** The "Apply standard penalty rule" bulk action from the spec is NOT shipped in this slice — it depends on W7 PENALTY-1 (default penalty rule, not yet built). No selection state, no checkboxes, no bulk bar. A follow-up slice will add these once PENALTY-1 ships.

### Added
- **CONTRACT-1 goddess contracts list filters, audit-log enrichment and sub-side next-payments card** (ref `planning/todo.md` W4, single `feat(contracts)` commit):
  - **Filter query params on `GET /goddess/debts`.** New repeatable `status: list[DebtContractStatus] | None` plus `sub_id: UUID | None`, `min_amount: Decimal | None`, `max_amount: Decimal | None`. 422 raised via `core.exceptions.Validation("min_amount must be ≤ max_amount", min_amount, max_amount)` so the `{error, message, context}` envelope is preserved — no `HTTPException` fallback. `daos/debt_dao.py::list_for_goddess_filtered` composes WHERE clauses only for params that are non-None; `amount` maps to `DebtContract.principal` (documented — the list surface exposes `principal` as the primary money column). Existing unfiltered callers pass nothing and behave unchanged.
  - **Audit log actor enrichment.** `schemas/debt.py::DebtContractAuditOut` gained `actor_display_name: str` (derived in the controller — `"system"` for null actors, `User.display_name` otherwise, falls back to `"unknown"` only when a stored actor UUID points at a deleted user). `from_attributes = False` so the schema can't be built from a raw `DebtContractAudit` row bypassing the derivation. `controllers/debt/controller.py::list_audit` now bulk-resolves actor IDs via new `UserDao.get_many_by_ids` (single `IN` query, zero N+1). The raw `actor_id: UUID | None` is still on the payload for admin surfaces; clients for sub / goddess render `actor_display_name` instead (UUID rule).
  - **Frontend filter bar.** `components/contracts/ContractFilters.tsx` (145 lines): status chip-buttons (multi-select, `aria-pressed={active.includes(s)}`), `SearchableSelect` for sub, and min / max GBP inputs with 400 ms debounced commit + inline `min > max` validation. Wired to `routes/goddess/GoddessContractsRoute.tsx` (155 lines) through the new `queryKeys.goddess.contractsList(filters)` key so every filter change generates a fresh cache bucket. `pending_dom_counter` status copy unified to `"Awaiting your counter-offer"` in `ContractStatusChip.tsx`.
  - **Next-payments card for subs.** `components/contracts/NextPaymentsCard.tsx` (62 lines) — mounts only on sub-viewed active contracts in `routes/ContractDetailRoute.tsx` (241 lines). Computes the next three instalment dates client-side from `last_payment_at ?? first_payment_at ?? created_at` plus `payment_frequency` (day / week / month with London wallclock shift). Hidden for non-active contracts. Goddess's "Preview contract" affordance is now gated by a `PRE_SIGNATURE_STATUSES` set so it's only visible before signature; PDF opens in a new tab via `window.open(url, "_blank", "noopener,noreferrer")` (pre-existing `env` import bug fixed — `downloadContractPdfApi` referenced `env.VITE_API_BASE_URL` without the import).
  - **Sub-scoped sub-detail tab enrichment.** `routes/goddess/subs/SubContractsTab.tsx` (136 lines) now queries with the `sub_id` filter param (trusting the server's scope — no redundant client-side `.filter((c) => c.sub_id === subId)` second source of truth) and adds `Monthly` + `Rate %` columns derived client-side (`deriveMonthly` / `deriveRate`). `routes/sub/SubContractsRoute.tsx` (139 lines) renders the `StatusCell` with a secondary `"Behind · £X · N period(s)"` line when the contract reports being behind.

### Added
- **INVITE-1 invitation table polish and email preview / resend** (ref `planning/todo.md` W4, single `feat(invitations)` commit):
  - **Derived `status` enum on `InvitationOut`.** New `InvitationStatus` (`str` `Enum`): `active | pending_entry_tribute_paid | consumed | expired`. Not stored — derived in `controllers/invitation_controller.py::_invitation_out` from `used_at`, `expires_at`, and the linked sub's `UserStatus`. `list_for_goddess` bulk-loads the linked users in a single `IN` query (new `UserDao.get_many_by_ids`) so derivation is zero-N+1.
  - **Resend endpoint.** `POST /goddess/invitations/{invitation_id}/resend` — 204, body `{ email: EmailStr }`. Uses the existing `invite.html` template rendered with `goddess_name` + `invite_url`, sends via `services.email.factory.get_email_service()` (invoked inside the handler to mirror the `auth.py` pattern). 404 when the invitation does not belong to the caller (not 403 — never leaks existence); 409 when the invitation is not `active`. Decorated with `@audit(kind="invitation_resent", entity="invitation")`.
  - **Preview endpoint.** `GET /goddess/invitations/{invitation_id}/preview` — 200, returns `InvitationPreviewOut { subject, html }`. Pure read, no audit. Same 404 rule as resend. Uses the exact same `render_template("invite.html", …)` call so the modal shows the email the sub will actually receive.
  - **Frontend table polish.** `routes/goddess/InvitationsListRoute.tsx` (199 lines, under 300 cap): new "Token" column with 8-char truncation + clipboard copy button (`navigator.clipboard.writeText` with success / failure toasts, `try/catch` so a denied clipboard permission surfaces `toast.error("Copy failed")` instead of throwing), unified `formatDateTime` helper producing `D Mon YYYY · HH:mm` in `en-GB` 24h, replacement `InvitationStatusChip` driven by the server-returned `status` enum (pink for active, amber `text-status-warning` for `pending_entry_tribute_paid`, muted for consumed, danger for expired), new "Actions" column with per-row "Preview" (always) + "Resend" (visible only when `status === "active"`) buttons.
  - **Modals.** `components/invitations/InvitationPreviewModal.tsx` (38 lines) — wraps the shared `Modal` primitive, fires `previewInvitationApi` via `useQuery` (gated by conditional mount — no always-mounted-hidden), renders the HTML inside `<iframe srcDoc>` with `min-h-[500px]` Tailwind arbitrary value so the email CSS can't bleed into the app. `components/invitations/InvitationResendModal.tsx` (66 lines) — email input + `useMutation`, maps 409 → `toast.error("Invitation is no longer active")`, other errors → `toast.error("Failed to send email")`. Pending state disables the button and shows a "Sending…" label.
  - **Create form polish.** `routes/goddess/InviteSubRoute.tsx` now wraps the amount input in a flex container with a leading `<span>£</span>` (accessible name isolation — the £ is a sibling span, not part of the `<label>`), submit button disables + shows a Tailwind spinner while the mutation is in flight, `copyUrl` gained a `try/catch` + failure toast.

### Added
- **WEEKLY-1 weekly intake route alignment with drill-down and CSV export** (ref `planning/todo.md` W4, single `feat(goddess)` commit):
  - **Route alignment.** Weekly intake now lives at `/goddess/payments/weekly` to match the spec's read-model pattern (`GET /goddess/payments/weekly`, role-prefixed). The old `/goddess/weekly` URL `<Navigate replace>` redirects to the new path so bookmarks survive. Sidebar `navItems.ts` and `queryKeys.goddess.weeklyPayments()` realigned.
  - **New read-model endpoint.** `GET /goddess/payments/weekly/{week_start}` returns `list[PaymentOut]` for the calling goddess whose `validated_at` falls inside the Europe/London week starting on `week_start` (Monday 00:00 local → Sunday 23:59:59.999999 local). DST-safe via `ZoneInfo("Europe/London")` — never hardcodes an offset. Path param validated as `yyyy-MM-dd`. Scoped by `PaymentDeclaration.goddess_id` so totals reconcile exactly with the weekly aggregate bucket, including historical payments from now-inactive subs (spec: weekly intake is a historical ledger, not a live-roster snapshot).
  - **Drill-down side panel.** Clicking a week row opens a new right-slide `components/ui/SidePanel.tsx` (overlay click / Esc / focus trap / previous-focus restore on unmount — reusable primitive, 103 lines). `components/goddess/WeeklyDetailPanel.tsx` (197 lines) queries the new endpoint, joins `sub_id → @username` via the existing `listGoddessSubsApi` cache (`staleTime: 60_000`), and renders one row per payment: display name + `@username`, GBP amount, `MethodIcon` + method name, category, a source `Badge` (`sub_declared` / `goddess_recorded` / `ingested`), and the London-formatted `validated_at` stamp. Empty / loading / error states reuse the shared `EmptyState` / `ListSkeleton` / `ErrorState` primitives. No raw UUIDs surfaced.
  - **CSV export.** `services/goddess/weeklyCsv.ts` (62 lines) builds a blob with header `Validated at (Europe/London),Sub,Username,Amount (GBP),Method,Category,Source,Note`. CSV-escapes `,` / `"` / `\n` / `\r` and doubles embedded quotes. Filename: `weekly-payments-<week_start>.csv`. Triggered from a footer `Export CSV` button on the side panel; disabled when the week has zero validated rows.
  - **WeekRowButton cleanup.** Dropped the unused `buttonRef` callback (focus restore is now fully owned by `SidePanel`'s previously-focused cleanup — no duplicated state in the route).

### Added
- **RECORD-1 record payment cash-night batch UX** (ref `planning/todo.md` W4, single `feat(goddess)` commit):
  - **Success toast with source badge.** `routes/goddess/RecordPaymentRoute.tsx` now fires a `sonner` `toast.success(<JSX>, { duration: 5000 })` after `recordPaymentApi` resolves. Toast content: `"Recorded £<amount> for <sub_display_name>"` + inline `<Badge variant="debt">Goddess-recorded</Badge>` on the right. No emoji. `toast.error("Failed to record payment. Check the form and retry.")` replaces the previous inline `<p>` error; the client-side amount-format `amountErr` inline message stays.
  - **Cash-night batch flow.** Submit now has two primary actions: `"Record & add another"` (primary, keeps the form open — clears `amount`, `externalTs`, `note`, `amountErr`; preserves `selectedSub`, `methodId`, `category`; focuses the sub picker trigger via `requestAnimationFrame`) and `"Record & close"` (secondary, preserves the pre-RECORD-1 navigate-to-`/goddess/validations` behaviour). `Cancel` still navigates away. Both primary actions share a single `submitWithMode("reopen" | "close")` dispatcher so the success branch picks the right follow-up.
  - **SearchableSelect ref plumbing.** `components/shared/SearchableSelect.tsx` grew two optional props (`triggerRef?: RefObject<HTMLButtonElement | null>`, `ariaLabel?: string`) and forwards them onto the `<button role="combobox">`. All five pre-existing callers unchanged — the new props are `undefined` by default.
  - **Type cleanup.** The pre-existing `Number(amount) as unknown as string & number` hack was removed — regenerated `RecordPaymentIn.amount` is typed as `number | string`, so `Number(amount)` now satisfies the union without any assertion. Zero new `as` casts in the slice.

### Added
- **VALID-1 goddess pending validations UX** (ref `planning/todo.md` W4, single `feat(ui)` commit):
  - **Inline proof thumbnail per row.** `components/payments/PendingValidationRow.tsx` renders a 72×72 `<img loading="lazy" decoding="async">` wrapped in an accessible `<button aria-label="View proof for <sub>'s £<amount> declaration">`. When `PaymentOut.proof_presigned_url` is null (legacy rows pre-DECLARE-1), a neutral dashed-border placeholder (`"no proof"`, `aria-label="No proof attached"`) renders in the same 72×72 footprint.
  - **Lightbox modal.** New `components/payments/ProofLightbox.tsx` (22 lines) wraps the existing `Modal` primitive (`size="xl"`) so Esc + overlay click + focus trap are inherited. Image constrained by `max-w-[90vw] max-h-[85vh] object-contain`. No download button; right-click is sufficient.
  - **Bulk approve.** Sticky header gets a tri-state `"Select all pending declarations"` checkbox (native `indeterminate` bit set via ref for the partial-selection case) plus an inline live count (`"2 of 2 selected"` / `"2 pending"`). Per-row checkbox on the left with `aria-label="Select declaration from <sub> for £<amount>"`. When ≥ 1 selected, a floating action bar (`role="region"`, `"Bulk validation actions"`) with `"Validate selected (N)"` fires `Promise.allSettled(ids.map(id => validateDeclarationApi(id, {})))` — deliberately `allSettled` so a single 409/500 doesn't roll back successes. On completion, the query key `queryKeys.goddess.pendingPayments()` is invalidated, selection clears, and partial failures surface as an inline `ErrorState` above the list (`"N of M validations failed: <first reason>"`). Reject stays per-row (reuses `RejectModal` from REJECT-1) — no bulk reject.
  - **Source badge preserved.** The `sub_declared | goddess_recorded | ingested` Badge keeps rendering on every row so the goddess can see the declaration origin post-validation. Rows displayed under the `Badge`: display name + GBP amount + category + declared-at + method icon + optional note. No raw UUIDs reach the DOM (UUID rule).
  - **Component split.** `PendingValidationsRoute.tsx` (209 lines, under the 300-line cap) now orchestrates the query + mutations + selection state only; row rendering lives in `PendingValidationRow.tsx` (164 lines) and the bulk affordances in `PendingValidationsBulkBar.tsx` (73 lines, exports both `PendingValidationsBulkBar` and `PendingValidationsSelectAll`). No new TanStack Query key; no `paymentsApi.ts` or backend touch.

- **DECLARE-1 sub declare hardening with mandatory proof upload** (ref `planning/todo.md` W4, single `feat(payments)` commit):
  - **Multipart proof upload.** `POST /sub/payments` switched from `application/json` to `multipart/form-data`. Required file field `proof: UploadFile` (jpeg / png / webp, ≤ 5 MB) plus `Form()` fields `category`, `amount` (Decimal-parseable string), `method_id` (UUID), optional `external_timestamp` (UTC-naive ISO), `note`, `target_id`. MIME whitelist is enforced before `await proof.read()` so a rejected MIME never hits the body read path (rejection order: 415 → 413 → 422 → 201). JPEG bodies are EXIF-stripped via Pillow re-encode; PNG/WEBP pass through untouched. Keys follow the existing photo convention: `<goddess_id>/<sub_id>/<declaration_id>.<ext>`, extension derived from MIME (never from user-supplied `file.filename`).
  - **New exception types.** `core/exceptions.py` now exposes `PayloadTooLarge` (413) and `UnsupportedMediaType` (415) `AppError` subclasses, wired into `core/exception_handlers.py` so every router can raise them and get the standard `{error, message, context}` envelope.
  - **Schema.** `models/payment.py::PaymentDeclaration` now has `proof_key: str | None` (nullable for legacy-row compat, populated on every new declaration). `schemas/payment.py::PaymentOut` carries a new `proof_presigned_url: str | None` — a 10-minute presigned GET URL generated per row in `controllers/payment/helpers.py::to_out`. The dashboard recent-payments list (`controllers/dashboard/controller.py`) now routes through `to_out` in an `asyncio.gather`, so every payment surface (sub history, goddess pending queue, dashboard recent-payments) serves the presigned URL consistently and signs the N rows in parallel.
  - **MinIO bucket.** `docker-compose.yml`'s `minio-init` step now `mc mb --ignore-existing local/payment-proofs`, alongside `sub-photos`, `toy-photos`, `vault`. Config: `Settings.s3_bucket_payment_proofs = "payment-proofs"` (env `S3_BUCKET_PAYMENT_PROOFS`).
  - **Migration.** `alembic/versions/59f9d4ae8d17_add_payment_declaration_proof_key.py` — single `add_column("payment_declaration", Column("proof_key", String, nullable=True))`, with matching `drop_column` on downgrade. Round-trip verified.
  - **Frontend form rebuild.** `routes/sub/PaymentFormRoute.tsx` (now 254 lines, down from a ballooning monolith) delegates to three new tiny components under `components/payments/`:
    - `CategoryRadioGroup.tsx` — radios with inline spec descriptions under each label (`entry`: "Your one-time gateway. Only available on first sign-in.", `tribute`: "A free offering outside your rolling obligation.", `rolling`: "Your weekly rolling tribute. Counts toward your rolling amount.") rendered in `text-base-text-subtle`.
    - `MethodPicker.tsx` — thin wrapper around `MethodIcon` + `METHOD_LABELS` so the form route stays declarative.
    - `ProofUploadField.tsx` — file input with MIME + size client-side validation, inline error, `URL.createObjectURL` thumbnail preview with cleanup, Replace / Remove affordances, `<label htmlFor>` + `aria-describedby` + `aria-invalid`.
  - **Amount defaults.** `kind=entry_tribute` query string OR `user.status === "pending_entry_tribute"` prefills from `user.entry_tribute_amount`; `category === "rolling"` prefills from `user.rolling_amount` (TODO marker for `/sub/me/rolling` when that endpoint lands); any other category → empty. A `userEditedAmount` flag preserves a value the user has already customised across category switches.
  - **Payment time default.** `"When did you pay?"` now defaults to Europe/London wallclock via a pure `nowLondonIsoNaive()` helper built from `Intl.DateTimeFormat("en-GB", { timeZone: "Europe/London" })` — no new deps. The DateTimePicker still receives and emits UTC-naive ISO strings, so the storage contract is unchanged; only the default display changes.
  - **Multipart API helper.** `services/payments/paymentsApi.ts` drops the obsolete JSON `declarePaymentApi` in favour of `declarePaymentMultipartApi` — raw `fetch` (bearer header, `Accept: application/json`, `credentials: "include"`, 60-second `AbortSignal.timeout`), mirrors the existing `/profile/photos` upload pattern (documented exception to the `openapi-fetch` convention because multipart support there is limited). Custom `DeclarePaymentHttpError` carries `status` + `detail`; the error-body parser now reads the `{error, message, context}` envelope (falling back to `{detail}` for older shapes).
  - **Copy.** Note placeholder → `"An offering note for your goddess (optional)."`. Submit button disables while uploading with a "Uploading…" label + spinner. Error toasts map 413 → "File too large (5 MB max).", 415 → "Unsupported file type.", 400 → backend `message`/`detail`.

### Fixed
- **AUDIT-1 audit log decorator coverage and seed entries** (ref `planning/todo.md` W3, single `fix(admin)` commit):
  - **Why.** `/admin/admin_actions` was empty after `make init-dbs` despite invitations / payments / breaches being seeded — either the seed didn't write or live actions didn't log. Legally risky; this slice closes both gaps.
  - **New cross-cutting decorator.** `server/decorators/audit.py::@audit(kind: str, entity: str | None = None)` — `functools.wraps`-based decorator that runs the wrapped FastAPI handler, and on 2xx return writes one `AdminAction` row via `AdminActionDao.record`. Resolves actor by kwarg name (`user | goddess | admin | caller | actor` — `sub` removed to avoid the target-as-actor footgun) and session by `session: AsyncSession` kwarg convention. `entity_id` derives from the return value's `.id` when present. Payload whitelist: UUID / datetime / str / int / float / bool / None path + query scalars; `BaseModel` body kwargs are `.model_dump(mode="json")`-serialised and merged under their kwarg name (`body`, `invitation_in`, …) with recursive redaction of `password` / `password_hash` / `signature_b64`. Decorator commits the audit row itself (handler owns its own transaction; audit row survives even if the handler flushed-but-didn't-commit). On 4xx/5xx the audit row is not written. Missing actor / session is logged via `logging.warning` (never raises — a broken audit must not 500 the user).
  - **60 audited routes across 20 routers.** `invitations.py`, `payments.py`, `debt_contracts.py`, `rolling.py`, `blacklist.py`, `adjustments.py`, `goddess_profile.py`, `rituals.py`, `tasks.py`, `toys.py`, `kinks.py`, `limits.py`, `merits.py`, `payment_methods.py`, `penalty_rules.py`, `tribute_minimum.py`, `journal.py`, `review_queue.py`, `goddess_photos.py`, `admin_cron.py`. Kind taxonomy is `snake_case_past_tense`: `invitation_created`, `payment_validated`, `payment_rejected`, `payment_recorded`, `contract_created`, `contract_counter_proposed`, `contract_accepted`, `contract_rejected`, `contract_closed`, `contract_clauses_updated`, `rolling_upserted`, `rolling_cleared`, `breach_applied`, `breach_forgiven`, `surprise_penalty_applied`, `adjustment_created`, `request_approved`, `request_rejected`, `fee_set`, `sub_profile_edited`, `sub_ownership_status_changed`, `ritual_*`, `task_*`, `toy_*`, `kink_proposal_*`, `limit_acknowledged`, `reward_*`, `punishment_*`, `payment_method_*`, `penalty_rule_*`, `tribute_minimum_*`, `journal_commented`, `review_queue_bulk_action`, `photo_approved`, `photo_rejected`, `cron_run`. Sub-owned mutation routes (sub's own `POST /sub/payments`, journal entries, etc.) are intentionally NOT audited — scope is staff actions only.
  - **Decorator signature preservation.** The wrapper keeps the wrapped handler's `inspect.Signature` so FastAPI's dependency-injection graph and OpenAPI `requestBody` / `responses` / `response_model` remain intact on every decorated route (verified live via `/openapi.json`).
  - **`/admin/admin_actions` filtering.** Endpoint now accepts `kind: str | None`, `actor_id: UUID | None`, `date_from: date | None`, `date_to: date | None` on top of the existing `search`, `page`, `page_size`. Date filters are inclusive and compare against UTC `created_at::date` (documented explicitly — the endpoint does NOT yet translate the query date to London-local; that's a follow-up if users ask). The schema `kind` field maps to the underlying `AdminAction.action` column (column name kept historical for migration safety — a comment in `models/admin_action.py` documents the drift).
  - **Seed rows.** `server/seeds/fake_data.py` now emits `AdminAction` rows for every relevant fake staff action: invitations (`invitation_created`), validations (`payment_validated`), live payment recordings (`payment_recorded`), rejections (`payment_rejected`), contract lifecycle (`contract_created`, `contract_closed`), breaches (`breach_applied`), adjustments (`adjustment_created`), rolling upserts (`rolling_upserted`), rituals (`ritual_created`), payment method bootstrap (`payment_method_created`), and limit acknowledgments (`limit_acknowledged` — three of the twelve seeded limits now carry `acknowledged_by_goddess_at` timestamps so the loop actually emits rows). Target was ≥ 50 rows; current count is **83**.

### Added
- **KPI-2 live sub dashboard and `/sub/approvals` alias** (ref `planning/todo.md` W3, single `feat(sub)` commit):
  - **Backend read-model endpoint.** New `GET /sub/dashboard/summary` (under `dashboards.sub_router`) returns `SubDashboardSummary` with eight fields: `pending_approvals_count` (int), `next_payment_amount` (`Decimal | None`, GBP), `next_payment_due_at` (UTC naive `datetime | None`), `late_rolling_amount` (`Decimal`, `0.00` when none), `late_contract_amount` (`Decimal`), `today_rituals_count` (int, Europe/London "today"), `today_open_tasks_count` (int, predicate matches existing `/sub/tasks`), `journal_streak_days` (int). Aggregation lives in a new `SubDashboardSummaryController` that fans out the six DAO calls via `asyncio.gather`.
  - **Contract-late math correctness.** `_compute_late_contract_amount` sums `minimum_payment` only for contracts where `current_period_index(c, now)` is NOT present in the bulk-fetched `DebtEventDao.paid_period_indices_for_contracts` set — a sub who paid this week's instalment no longer sees a non-zero "Late" tile for the rest of the period. The goddess `DashboardSummaryController` was refactored to reuse the same bulk DAO helper, eliminating the N+1 pattern there too.
  - **Timezone discipline.** Period-start + "now" are both converted to `Europe/London` before `.date()` comparison so the "late" boundary lines up with the business clock (previously `now.date()` used UTC). A shared `controllers/dashboard/helpers.py::now_utc()` helper is now imported by both summary controllers.
  - **Journal streak bounded.** `JournalDao.current_streak_days` caps the lookup window at the last 90 days (`_STREAK_LOOKBACK_DAYS`). Docstring notes that longer streaks are truncated — acceptable for the MVP dashboard.
  - **Approvals route alias.** `GET /sub/approvals` returns a `308 Permanent Redirect` to `/sub/adjustments/pending` (under `debt-contracts` Swagger tag for grouping). Behind `require_role(UserRole.sub)` so unauthenticated callers still get `401`, not a leaked redirect.
  - **New DAO methods.** `AdjustmentDao.count_pending_for_sub`, `RitualOccurrenceDao.count_today_for_sub(sub_id, date)`, `TaskDao.count_open_or_submitted_for_sub`, `JournalDao.current_streak_days` (90-day capped), `DebtEventDao.paid_period_indices_for_contracts(contract_ids) -> dict[UUID, set[int]]` (bulk loader).
  - **Frontend consumption.** New `services/dashboards/subDashboardApi.ts::getSubDashboardSummaryApi()` via `openapi-fetch` against regenerated `api.generated.ts`. New `hooks/dashboard/useSubDashboardSummary.ts` (`queryKeys.sub.dashboardSummary()`, `staleTime: 30_000`, `refetchOnWindowFocus: false`). `types/dashboard.ts` exposes `SubDashboardSummary` as a direct re-export of the generated schema — no hand-written bridge.
  - **Welcome (`/`) becomes a 4-tile sub dashboard.** New `components/dashboard/SubSummaryGrid.tsx` renders: **Pending approvals** (count, linked to `/sub/adjustments`, sublabel `"awaiting goddess"` when > 0), **Next payment** (GBP via `Intl.NumberFormat("en-GB", { style: "currency", currency: "GBP" })`, sublabel `"in N d"` within 30 days, `"on <en-GB date>"` beyond, `"due today"` for `diffDays <= 0`, `"None scheduled"` when null), **Streak** (count + `"day streak"` / `"days streak"` / `"no streak yet"`, linked to `/sub/journal`), **Late** (summed rolling + contract via `addGBPDecimalStrings` BigInt-pennies arithmetic — no `parseFloat` on Decimals; `border-status-danger` when non-zero). Tiles use Tailwind 4 `tabular-nums` for stable digit width.
  - **Today empty-state copy.** `routes/sub/TodayRoute.tsx` rituals empty state now reads `"No ritual tonight — rest. Tomorrow she remembers."` (was `"No rituals scheduled for today."`). Message prop omitted; `EmptyState` component guards against missing `message`.
  - **Format helpers.** New `services/dashboards/subDashboardFormat.ts` — `formatGBP(decimalString)`, `formatNextPaymentDue(dueAt)` (appends `"Z"` to UTC-naive strings before `new Date()`, branches on `diffDays <= 0 | <= 30 | else`), `addGBPDecimalStrings(a, b)` (BigInt pennies, zero-pad fractional to 2 digits, preserves sign).

- **KPI-1 live dashboard counters with active-only labels** (ref `planning/todo.md` W3, single `feat(goddess)` commit):
  - **Backend read-model endpoint.** New `GET /goddess/dashboard/summary` (under `dashboards.goddess_router`) returns `DashboardSummary` with twelve integer counters: `subs_active`, `subs_paused`, `contracts_active`, `contracts_closed`, `invitations_active`, `invitations_consumed`, `validations_pending`, `validations_oldest_age_h`, `late_rolling_count`, `late_contract_count`, `photo_queue_count`, `profile_change_requests_count`. Aggregation lives in a new `DashboardSummaryController` that fans out the queries via `asyncio.gather`. Counts reuse the same predicates the list endpoints use (`/goddess/late`, `/goddess/validations`, `_contracts_late_map` from the existing dashboard, etc.) — one source of truth per counter. `subs_paused` is hardwired to `0` with a schema-field note (no `paused` member on `UserStatus` yet; reserved for forward compatibility).
  - **New DAO count methods.** `UserDao.count_subs_by_status` + `list_active_subs`; `DebtContractDao.count_by_status` + `list_active_for_goddess`; `InvitationDao.count_active` + `count_consumed`; `PaymentDao.count_pending_validation` + `oldest_pending_validation_age_hours` (returns `0` when none); `SubPhotoDao.count_pending_review`; `ProfileChangeRequestDao.count_pending`; `RollingTributeDao.list_for_sub_ids` (single-shot bulk loader — prevents N+1 in the summary controller's late-rolling count).
  - **Auth gate.** Endpoint is scoped to `require_role(UserRole.goddess)`; sub and admin callers receive 403. Admin impersonation of a goddess passes through naturally (no special-casing).
  - **Frontend consumption.** New `services/dashboards/goddessDashboardApi.ts::getGoddessDashboardSummaryApi()` using `openapi-fetch` against regenerated `api.generated.ts`. New `hooks/dashboard/useGoddessDashboardSummary.ts` (query key `queryKeys.goddess.dashboardSummary()`, `staleTime: 30_000`, `refetchOnWindowFocus: false`). New `hooks/dashboard/useGoddessDashboard.ts` extracts the main dashboard query out of the route component so the data-layer setting lives in a hook, not the route.
  - **Welcome (`/`) + dashboard (`/goddess/dashboard`)** both consume the new summary via a shared `components/dashboard/DashboardSummaryGrid.tsx` (8-tile grid). Tiles: Subs (active), Contracts (active) + closed sublabel, Invitations + consumed sublabel, Pending validations + freshness indicator, Late (rolling), Late (contracts), Photo queue, Profile change requests (borders `border-pink-primary` when count > 0, otherwise `border-base-border` — fulfils the PLACEHOLDER-1 KPI-1 TODO).
  - **Freshness helper.** New `services/dashboards/freshness.ts::hoursToFreshnessLabel` — returns `"oldest X d Y h"` for multi-day, `"oldest X h"` for sub-day, `"oldest < 1 h"` for `pending > 0 && age_h === 0`, nothing when `pending === 0`. Pure, testable (phase 10).
  - **Labels.** Every ambiguous "Subs" / "Contracts" heading on the welcome + dashboard pages is now the explicit "Subs (active)" / "Contracts (active)" to remove the "does this include paused / closed?" question.
  - **Charts: eager load, no focus refetch.** Both `useGoddessDashboardCharts` and the new `useGoddessDashboard` set `refetchOnWindowFocus: false` at the data layer. Charts render directly on mount (no "Load chart" CTA gating — none existed in the tree, but verified).
  - **Typography.** KPI numbers use the Tailwind 4 `tabular-nums` utility (resolves to `font-variant-numeric: tabular-nums` per the generated CSS) so live-updating numbers don't jitter when digits change width.
  - **Error surfaces.** Both the welcome home (`GoddessHome` in `HomeRoute.tsx`) and `/goddess/dashboard` consume `isError` from the summary hook and render an `ErrorState` on failure instead of silently showing nothing.

### Fixed
- **PLACEHOLDER-1 strip shipped placeholders and joke alts** (ref `planning/todo.md` W2, single `fix(ui)` commit):
  - **Goddess sub Late tab.** `routes/goddess/subs/SubLateTab.tsx` no longer renders the "coming soon" stub. Now composes a sortable table from the existing rolling-late feed (`getLateSubsApi`) and the contract-late subset of `listGoddessDebtsApi` (`on_track === false && status === "active"`), filtered to the open sub. Columns: Type (Rolling/Contract badge), Amount (GBP), Days late, Since date (en-GB). Sort state is local, default by days-late desc. Empty state reads "No late items for this sub." Composition in the client is acknowledged as a seam — a future `GET /goddess/subs/{slug}/late` scoped endpoint would move the aggregation server-side (follow-up only, not needed for this slice).
  - **Date locale.** `client/index.html` now sets `<html lang="en-GB">` (was `lang="en"`). Fixes the native `<input type="date">` placeholder from the browser default `jj/mm/aaaa` (French, derived from missing lang) to `dd/mm/yyyy` on `/profile` DOB and every other date input app-wide. This also aligns the app-level locale for any downstream `Intl` defaults.
  - **Avatar helper + alt discipline.** New `components/profile/Avatar.tsx` — named export `Avatar({ user, size?, className? })` that coerces `user.avatar_key` (incl. null/unknown) through `AVATAR_KEYS` to a safe `AvatarKey` and delegates to `AvatarImage`, passing `user.display_name` as the alt text. `AvatarImage` now takes an optional `displayName` prop and renders `alt={displayName ?? ""}` — no more `alt={entry.label}` (was "Piggy bank", "Open wallet", etc., which is the illustration's label, not the user's identity). Migrated call-sites where a user object is in scope: `SubProfileCard`, `SubsListRoute`, `RecordPaymentRoute`, `ProfileRoute`. Preview/picker call-sites genuinely not tied to a user identity (`SubProfileTab` avatar picker "Current selection", `ChangeRequestList` proposed-avatar preview, `ProfileRequestCard` current+proposed preview) stay on `AvatarImage` and render as decorative (`alt=""`).
  - **ESLint rule.** `client/eslint.config.js` adds a `no-restricted-syntax` selector catching `<img alt="...">` hardcoded literals containing `/avatar|user|goddess|sub/i`, with an error message pointing to `<Avatar user={…} />`. Expression-based alts (`alt={x}`) are intentionally allowed — the rule is a copy-paste trap, not a structural enforcement (the structural rule lives in the `<Avatar>` helper existing as the call-site convention). `pnpm lint` clean.
  - **Verified already-correct:** `HomeRoute.tsx` welcome heading was already dynamic (`Welcome, {user.display_name}`) before this slice. The goddess dashboard's "Profile change requests" tile was already using `border-base-border` — no stuck pink border found in the tree. A single-line `// TODO(KPI-1): highlight tile when pending_change_requests > 0` marker was added above the `ActionGrid` component as a deferred intent (KPI-1 W3 owns the summary endpoint that would drive the highlight).
  - **Follow-up hygiene.** `SubLateTab` originally computed `const TODAY = new Date()` at module load — stale after midnight. Replaced with `Date.now()` at call-time inside `daysSince`.

### Added
- **ENTRY-1 porch layout for pending-entry-tribute subs** (ref `planning/todo.md` W2, single `feat(auth)` commit):
  - **Backend porch gate.** New `server/dependencies/porch_gate.py::porch_gate` FastAPI dependency, wired globally via `app = FastAPI(..., dependencies=[Depends(porch_gate)])` in `server/main.py`. Blocks all requests from users whose `status == pending_entry_tribute` unless `(request.method, request.url.path)` is on `_PORCH_ALLOWLIST` — a `frozenset[tuple[str, str]]` containing `(GET, /auth/me)`, `(POST, /auth/login|logout|refresh|password-reset/{request,confirm})`, `(POST, /sub/payments)`, `(GET, /health)`. Method-scoped so `GET /sub/payments` (history) stays blocked while `POST /sub/payments` (entry-tribute declaration) is reachable. Returns `HTTP 403` with `{error, message, access_state}` body + response header `X-Access-State: pending_entry_tribute`. Unauthenticated / malformed-token / non-porch-status requests pass through untouched. Admin impersonation bypass: when the decoded JWT carries `imp`, the gate returns early so an admin impersonating a porch sub retains full-app access.
  - **CORS expose.** `CORSMiddleware` now lists `expose_headers=["X-Access-State"]` so the browser can actually read the gate header on cross-origin 403 responses.
  - **Entry-tribute amount on /auth/me.** `schemas.auth.UserOut` grows `entry_tribute_amount: Decimal | None`. `AuthController` takes an optional `InvitationDao` and exposes `get_entry_tribute_amount(user_id) -> Decimal | None` — fetches the invitation that was used by the current user and returns its `entry_tribute_amount`. `auth.router.me` calls the controller only when `ctx.user.status == pending_entry_tribute`; None otherwise. New `InvitationDao.get_by_used_by_user_id(user_id)` query. All DB access funnelled through the controller — no DAO import in the router.
  - **Frontend porch shell.** New `components/layout/PorchLayout.tsx` — full-screen single-card layout showing entry-tribute amount in `Intl.NumberFormat("en-GB", { style: "currency", currency: "GBP" })` with a primary CTA "Declare entry tribute" → `/sub/payments/new?kind=entry_tribute`. New `components/layout/PorchGuard.tsx` wraps `ProtectedRoute` children and redirects to `/porch` when `user.status === "pending_entry_tribute"` and the current path is not on the sub-porch allowlist (`/porch`, `/sub/payments/new`). New `routes/sub/PorchRoute.tsx` — auto-navigates to `/sub/dashboard` once the status flips to `active` (covers the post-payment flip without a hard reload).
  - **Payment form adapter.** `routes/sub/PaymentFormRoute.tsx` — `kind=entry_tribute` query param locks the form to `category=entry`, prefills `amount` from `user.entry_tribute_amount`, disables non-entry category radios, and redirects back to `/porch` on submit (instead of `/sub/payments`).
  - **Router wiring.** `router.tsx` adds `/porch` as a `noLayout` protected route; `/pending-entry-tribute` redirects to `/porch`. `routes/public/SignupRoute.tsx` now navigates to `/porch` post-signup. Orphan `routes/sub/PendingEntryTributeRoute.tsx` deleted.
  - **Types.** `client/src/types/api.generated.ts` regenerated via `make sync-types` so `UserOut.entry_tribute_amount` is first-class on the client.

### Changed
- **CONSENT-1 feature-flag the medical module** (ref `planning/todo.md` W2, single `chore(profile)` commit):
  - **Backend.** New `MEDICAL_FEATURE_ENABLED` setting in `core.config.Settings` (default `False`). New `dependencies/feature_flags.py::require_medical_feature` raises 404 when flag is off. Wired at router level (`dependencies=[Depends(require_medical_feature)]`) on both `sub_router` (`/profile/medical` GET + PUT) and `goddess_router` (`GET /goddess/subs/{id}/medical`) in `routers/sub_medical.py`. Router-level placement guarantees the 404 fires before any role / consent / DAO work, satisfying the "no data ever decrypted or stored when disabled" intent.
  - **Frontend.** New `services/featureFlags.ts` exports `MEDICAL_FEATURE_ENABLED`, fed by the Zod-validated env schema in `utils/env.ts` (added `VITE_MEDICAL_FEATURE_ENABLED: z.enum(["true","false"]).default("false")`). `MedicalRoute` renders a neutral placeholder page ("Medical module coming soon — your data has not been collected.") when the flag is off. Medical entry is stripped from `SUB_NAV` and the goddess `MedicalRevealPanel` is hidden on `SubOverviewTab`.
  - **Layout refactor follow-up.** `components/layout/AppLayout.tsx` exceeded the 300-line ceiling after the flag wiring — nav arrays extracted to `components/layout/navItems.ts` (now 260 lines).
  - **Env defaults.** `VITE_MEDICAL_FEATURE_ENABLED=false` in `client/.env.example`; `MEDICAL_FEATURE_ENABLED=false` in `server/.env.example`.
  - **Copy deferred.** Per Q10-bis, legal medical-consent copy is deferred to backlog slice `NTH-medical-copy`; flag flipping to `true` is the trigger.
  - **Known follow-ups (non-blocking).** Router-level `dependencies=[Depends(...)]` adds an implicit 404 to the OpenAPI doc for those routes — explicit `404: _E404` entries in the per-endpoint `responses={}` are now slightly redundant but kept for discoverability. Prettier/ruff also reformatted two untouched lines (`client/src/lib/queryKeys.ts`, `server/controllers/payment/controller.py`) as part of the `make fmt` pass — carried in the same commit for tidiness.

### Fixed
- **REJECT-1 reject reason min length + stylish reject modal** (ref `planning/todo.md` W2, single `fix(ui)` commit):
  - **Backend validation.** Four goddess reject surfaces now require a reason of ≥5 chars and ≤500 chars:
    - `RejectIn.reason` (payment reject, `POST /goddess/payments/{id}/reject`) — was `str | None, default=None`, now required `str, min_length=5, max_length=500`. `PaymentController.reject` signature narrows to `str`.
    - `SubPhotoRejectIn.reason` (photo reject) — `min_length=1` → `5`.
    - `GoddessRejectIn` (profile change request reject) — field renamed `note` → `reason`, required, min 5. Router + controller updated; DB column name unchanged (`resolution_note`).
    - `BulkActionIn` (review queue bulk) — new `@model_validator(mode="after")` enforces reason present and `len(.strip()) ≥ 5` when `action=reject`. Dead controller-level `HTTPException(422)` fallback removed (schema now owns the check, restoring router→controller→DAO discipline).
  - **OpenAPI hygiene.** `reject_request` (`goddess_profile.py`) had a stale `400` entry in `responses={}` — no code path emits 400 — removed. `reject_declaration` adds `422: _E422`. Every reject route still enumerates its full non-2xx set.
  - **Notification body regression fix.** Payment reject notifier now sends `"Your payment of £{amount} was rejected: {reason}"` instead of dumping the raw reason as the body (surfaced by reviewer during the must-fix pass).
  - **Frontend.** New shared `client/src/components/shared/RejectModal.tsx` (focus on mount, inline min-5 validation, `{len}/500` counter, submit-error surface, `role="dialog"` + `aria-modal` inherited from `Modal`, Esc closes, named export, no `as` assertions). Migrated four call sites behind the shared modal:
    - `PendingValidationsRoute` — local ad-hoc modal deleted; payload now always passes `{ reason }` (required server-side).
    - `PhotoReviewCard` — local ad-hoc modal deleted.
    - `RejectRequestDialog` — shadcn `Dialog` implementation replaced; payload uses `{ reason }` (matches rename).
    - `BulkActionBar` — inline expand-panel replaced by the shared modal overlay.
  - **Sub-side feed visibility.** `rejection_reason` now renders on rejected items in `RitualCard` and `TaskCard` (new render) and was already shown in `PaymentHistoryRoute` and `ChangeRequestList`. `services/today/todayApi.ts` schemas widen `RitualOccurrenceSchema` + `OpenTaskSchema` with `rejection_reason`.
  - **Known follow-ups (non-blocking).** Sub does not yet have a profile-photo review-history surface, so photo rejection reasons are backend-captured but never shown to the addressed sub. Candidate for a dedicated `SubPhotoHistoryRoute` in a future slice. `api.generated.ts` for reject routes no longer exposes a typed `HTTPValidationError` body for 422 (consequence of `_E422` carrying only a description, not a schema) — nit, not a regression of behaviour.
  - Playwright walk verified on http://localhost:4010: goddess reject payment (£120 Dan declaration) — modal opens with `role="dialog"`, Reject disabled at 0 and 3 chars, inline error "Reason must be at least 5 characters." shown at 3 chars, enables at 28 chars, submit dismisses; photo reject modal on `/goddess/photo-queue` shares the same shell (disabled empty, counter `0/500`); sub `/sub/payments` shows `Rejected: Amount wrong — expected £100` on the card and notification popover reads `Your payment of £120.00 was rejected: Amount wrong — expected £100`. Review queue + profile change requests empty in seed — not walked through, but the shared-component migration alone was verified via code review.

### Fixed
- **MONEY-1 confirm + preview modals on destructive money actions** (ref `planning/todo.md` W2, single `fix(payments)` commit):
  - **Backend.** Four new preview endpoints in `server/routers/money_previews.py`:
    - `POST /goddess/contracts/{slug}/surprise-penalty/preview` — non-mutating balance projection.
    - `POST /goddess/contracts/{slug}/surprise-penalty` — commit with `confirmed_at` stamp; creates `ContractAdjustment(kind=surprise_penalty)`, emits `DebtEvent(surprise_penalty)`, writes `DebtContractAudit(event_type=surprise_penalty)`, notifies the sub.
    - `POST /sub/contracts/{slug}/buyout/preview` — pro-rated exit-amount quote (`exit_amount × elapsed/duration`).
    - `POST /goddess/subs/{username}/breach/preview` — returns cascade count + rolling balance that would be frozen.
  - **Schemas.** New `ContractAdjustmentKind` enum on `ContractAdjustment.kind` (nullable, back-compat). New Postgres enum value `surprise_penalty` on `debt_contract_event_type` via `money1_surprise_penalty_enum` migration (down_revision `148fb461728b`, uses `autocommit_block` for `ALTER TYPE ... ADD VALUE`). New `BreachPreviewIn` accepts empty reason (`min_length=0`) for lazy preview; commit-step breach still requires a real reason.
  - **Layer discipline.** `BlacklistController.breach` + `breach_preview` refactored to call new `DebtContractDao.list_active_for_sub(sub_id)` instead of raw `session.execute`. No controller holds SQL any more.
  - **Frontend.** New `ConfirmActionModal` (3 variants: `preview`, `typedConfirm`, `simple`) with focus trap, Esc close, click-outside, aria-labelledby, discriminated-union props (no `as` assertions). Built on top of it:
    - `SurprisePenaltyFlow` (3-step wizard: amount → preview → typed reason ≥5 chars).
    - `BuyoutPreviewModal` (sub-side, click-to-fetch quote).
    - `DangerZonePanel` on `SubOverviewTab` — breach action behind typed-username confirm + reason ≥5 chars gate; preview query keyed on `(username, reason)` to avoid stale closure.
  - **Wired removal.** Old one-click `SurprisePenaltyDialog` deleted; `ContractDetailRoute` now mounts `SurprisePenaltyFlow` only, so there is no preview bypass.
  - **Admin safe-mode.** `AdminCronRoute` "Run cron now" button wired as `disabled={true}` with hover title "Disabled pending admin safe-mode" pending W7 `CRON-1`.
  - Playwright walk verified on http://localhost:4010: surprise penalty 3-step flow (amount £50 → preview £890.55→£940.55 → reason <5 blocks apply → reason "late payment 3 times" applies, balance moves to £946.48 and audit log 2→3 events; sub notification received); admin cron button `[disabled]`; sub-side buyout quote (balance £946.48 − £321.48 discount = £625 exit); breach danger zone (button disabled until reason ≥5 chars, preview shows 1 cascade / £946.48 freeze / Yes blacklist).

### Fixed
- **UUID-1 hide raw UUIDs from goddess and sub UIs** (ref `planning/todo.md` W2, single `fix(security)` commit):
  - **Backend.** `DebtContract` gains a `slug` column (format `c_<6-char Crockford-base32>`, unique, indexed) generated by `generate_contract_slug()` at row creation. `DebtContractOut` exposes `slug` natively. Three new slug/username lookup routes:
    - `GET /goddess/contracts/by-slug/{slug}` (goddess role, ownership-checked, 404 on miss).
    - `GET /sub/contracts/by-slug/{slug}` (sub role, contract-belongs-to-sub check).
    - `GET /goddess/subs/by-username/{username}` (goddess role, sub-belongs-to-goddess check).
    All new routes carry `summary`/`description`/`response_model`/`status_code`/`tags`/`responses` per `CLAUDE.md`.
  - **Migration `a1b2c3d4e5f6_add_slug_to_debt_contract`** chains off `b25a7ef727dd`. Three-phase pattern: (1) add nullable column, (2) Python backfill loop with `SELECT ... FOR UPDATE` + per-row `generate_contract_slug()` (imported from `models.debt` so backfill + runtime share one alphabet), (3) `op.create_unique_constraint("uq_debt_contract_slug", …)` + `op.create_index("ix_debt_contract_slug", …, unique=False)` + `ALTER COLUMN … SET NOT NULL`. `downgrade()` drops index + constraint + column. `alembic downgrade -1 && alembic upgrade head` round-trip is green.
  - **Notification body formatters** — `panic_controller.py`, `services/cron/contracts.py`, `controllers/debt/helpers.py` (new `sub_notification_label(user)`) + `controllers/debt/controller.py` now emit `<display_name> (@<username>)` instead of bare `@<username>`. Fallback to `@<username>` when `display_name` is unset. Old seeded notification rows retain the old body format (acceptable: new notifications use the new format going forward).
  - **Frontend routes** — `/goddess/subs/:subId` → `/goddess/subs/:username`; `/goddess/contracts/:contractId/preview` → `/goddess/contracts/:slug/preview`; `/sub/debts/:contractId/sign` → `/sub/debts/:slug/sign`; `/debts/:contractId` → `/debts/:slug`. Every internal `<Link to>` + `navigate()` call across `routes/` and `components/` updated.
  - `ContractDetailRoute` dispatches by role — admin hits the UUID route, goddess hits `getContractBySlugGoddessApi`, sub hits `getContractBySlugSubApi`. `ContractPreviewRoute` same — PDF download button disabled until the query resolves, then uses the resolved UUID for the UUID-typed `/debts/{id}/pdf` endpoint (never concatenates the slug).
  - `LatePaymentList` — contract-late rows resolve slug via a new `useContractSlugMap` hook that joins with `queryKeys.goddess.contracts()`; rows render as plain `<span>` when slug is not yet cached (never a UUID `href`). TODO noted for LATE-1/KPI-1 to ship `contract_slug` on `LatePaymentItem` and drop the hook.
  - `BlacklistRoute` — cards render `<display_name> (@<username>) · £<balance> · <reason>` (was: UUID).
  - `SubManageRoute` — tabs mount only after `sub?.id` resolves (prevents empty-string child queries hitting `/goddess/subs//tribute-minimum/gauge`).
  - `PhotoReviewCard` — `@<username>` rendered, UUID fallback removed.
  - `client/src/types/api.generated.ts` regenerated via `pnpm sync-types`. `DebtContractOut` intersection `& { slug?: string | null }` removed (native now). `LatePaymentItem`/`LateSubItem` keep a hand-extension for `sub_username` until LATE-1/KPI-1 widen those DTOs server-side — flagged inline.
  - Playwright walk verified: `/goddess/subs/sub_chris`, `/debts/c_sf3hhm`, `/goddess/blacklist` (Eli Reeve card format), notifications popover, `/goddess/late`, goddess dashboard — zero UUIDs rendered on any goddess surface.

### Added
- **DROPDOWN-1 shared `SearchableSelect`** (ref `planning/todo.md` W1, single `feat(ui)` commit):
  - New `client/src/components/shared/SearchableSelect.tsx` (249 lines) — generic combobox with type-to-filter input, arrow-key navigation, Enter/Escape/Tab handlers, click-outside-to-close (mousedown), full aria (`combobox` + `listbox` + `option`). `nullable` prop renders a "clear" pseudo-option at the top for pickers where no selection is valid state.
  - `RecordPaymentRoute` sub picker migrated — typed subs list, avatar + `@username` in each option, inline state badge (ACTIVE / PENDING ENTRY TRIBUTE / BLACKLISTED). No change to the submission pipeline.
  - `PenaltyRuleForm` sub override migrated — UUID text input replaced by `SearchableSelect` with `nullable=true` (top entry = "All subs (no override)", highlighted). Resolves the `LOOSE-2` follow-up from `Docs/prior_slice_review.md`.
  - `PenaltyRuleList` now passes the `subs` prop through; `PenaltyRulesRoute` fetches via existing `queryKeys.goddess.subs()` — no new query key, no duplicate fetch.
  - `JournalReaderRoute` admin filter migrated in the same component (admin had ≤5 options — cheap win, keeps the pattern consistent across the goddess area).
  - Playwright walk verified on http://localhost:4010: Record Payment opens dropdown with 5 subs + search working; Penalty rules opens with "All subs (no override)" pinned at top and sub list below.

### Changed
- **SEED-1 dev seed refonte** (ref `planning/todo.md` W0, single `chore(db)` commit):
  - `server/seeds/fake_data.py` rewritten as a thin orchestrator wired around a frozen cast and deterministic timelines. All timestamps anchor to `FROZEN_TODAY = date(2026, 4, 17)` — no `datetime.now()` in seed code. Every re-run of `make feed-dbs` is guarded by a `sub_chris` existence check (idempotent), while `make init-dbs` continues to drop+recreate.
  - New `server/seeds/cast.py` — frozen `CAST` dict of the 6-pack (`sub_chris`, `sub_dan`, `sub_ben` active; `sub_invite_alex` pending entry tribute; `sub_invite_jordan` invitation-only, no User row yet; `sub_eli` blacklisted).
  - New `server/seeds/timeline.py` — pure date-series helpers: `chris_rolling_dates` (22 Mondays), `dan_rolling_dates` (14 irregular Fridays), `ben_rolling_dates` (8 irregular, last 7 d before `FROZEN_TODAY`), per-sub journal date generators, photo upload schedules, `frozen_dt` / `frozen_now` deterministic UTC helpers.
  - New `server/seeds/content.py` — hand-written English content pools: `JournalSpec` dataclass, 3 showcase journal entries with emotional grain (fear / pride / overwhelm), 12 + 6 + 9 + 4 body pools for Chris / Dan / Ben / Eli, 5 realistic reject reasons, 3 review-queue motivations, 3 mantras — no Faker generic.
  - KPI targets from `revue.md` §P0 verified live against the goddess UI (dashboard / subs / late / invitations / validations / photo queue / blacklist): `subs active = 3`, `invitations = 2`, `contracts = 3` (2 active + 1 closed), `late tonight = 1` (Ben, 7 d), `photo queue = 2` (Dan + Ben), `validations = 1` (Dan sub-declared), `blacklist = 1` (Eli). Ben's prior-dispute narrative is carried by Chris's existing pending `ContractAdjustment` (penalty waiver) — the stray `ProfileChangeRequest` seed from the first pass (which populated no valid column) was removed.
  - `server/pyproject.toml` adds `E501` ignore for the four seed files (matches the existing `seeds/kinks.py` treatment).

### Added
- **P1.5 J4 medical activation** (closes Phase J, ref `Docs/plan_post_wave7.md`):
  - Backend: `GET|PUT /profile/medical` (sub-only, gated by `require_consent("medical")`) and `GET /goddess/subs/{sub_id}/medical` (goddess-only, plaintext reveal). The 5 fields (`blood_type`, `allergies`, `medications`, `emergency_contact`, `medical_notes`) round-trip through `services/crypto/envelope.encrypt_for_goddess()` into the existing D6 `*_enc bytea` columns; AAD is `b"sub_medical:<sub_id>:<field>"` so any cross-field/cross-sub copy fails the AES-GCM tag. Sub `GET` returns `is_set` booleans only — plaintext never echoes back. Each goddess reveal records `admin_action(action="medical_reveal", entity="sub_medical", entity_id=<sub_id>)`. No new migration — `sub_medical` was created in wave-6 D6.
  - Frontend: `/profile/medical` route with 5-textarea editor + per-field "set" badges. New `MedicalConsentGate` intercepts the 428 from `require_consent`, renders the markdown body, posts to `/consent/medical/accept`, and resumes the original request — works on both initial GET and PUT-time consent expiry. Goddess `MedicalRevealPanel` mounted on `/goddess/subs/:subId` with explicit "Reveal medical info" button (no auto-fetch on mount), an inline "logged" banner, and a "Hide" toggle — plaintext lives in component state only, never in TanStack cache or localStorage.
  - `medicalKey` migrated from inline factory to `queryKeys.medical.own()`. `SUB_NAV` gains "Medical" (last entry).

### Added
- **P1.4 identity completion + aftercare** (B2 backend + B2 frontend + F6 full-stack, single commit, ref `Docs/plan_post_wave7.md`):
  - **B2 backend** identity fields: `User` model gains `gender`, `pronouns`, `location`, `timezone`, `date_of_birth`, `real_name` (all nullable). `SignupRequest` requires `timezone` (IANA) + `date_of_birth` (≥18 enforced via `@field_validator`); the rest are optional. `UserOut` exposes the new fields; `ProfileUpdate` accepts them all as optional. `real_name` edits route through the wave-3 `ProfileChangeRequest` flow once the field is non-null and a different value is sent — `PATCH /me/profile` returns `202 {"change_request_id"}` instead of `200 UserOut`. Goddess `_apply_diff` writes `proposed_real_name` onto the user on approval. `GET /goddess/subs` items include `real_name` (goddess-only). Migration `7aa2f3dc61ea_p1_4_b2_user_identity_fields` chains off `b250c3fd8787`.
  - **B2 frontend**: `pnpm sync-types` regenerated `api.generated.ts`. `SignupRoute` extended with 6 new inputs (timezone pre-filled via `Intl.DateTimeFormat().resolvedOptions().timeZone`, `<input type="date">` ≥18 client-side guard, optional gender/pronouns/location/real_name) — identity inputs delegated to the new `SignupIdentityFields` subcomponent (128 lines, keeps `SignupRoute` ≤300). New `IdentityFieldsCard` mounted in `ProfileRoute`; `updateProfileApi` now returns a discriminated union `{kind:"applied";user} | {kind:"pending_change_request";changeRequestId}` so the card shows a "pending goddess approval" inline note + toast on the 202 branch instead of optimistically updating cache. `GoddessSub` type extended with `real_name?: string | null`.
  - **F6 full-stack** aftercare: new `sub_aftercare` table (PK=`sub_id`, free-text `needs|comfort_items|contact_phrase|notes`), DAO upsert via `pg_insert(...).on_conflict_do_update(...)`, `GET|PUT /profile/aftercare` (sub-only). Frontend `AftercareRoute` at `/profile/aftercare` with the edit form; `AftercarePanel` renders on the sub dashboard whenever `localStorage["aftercare:session:<sub_id>:until"]` is in the future (polled every 60 s by `useAftercareActive`); goddess `SessionCompleteToggle` on `SubOverviewTab` sets that key for 30 minutes. Inline `aftercareKey` migrated to `queryKeys.aftercare.own()`. Migration `b25a7ef727dd_p1_4_f6_sub_aftercare` chains off `7aa2f3dc61ea` (linear, no merge needed).
  - Nav: `SUB_NAV` gains "Aftercare" (last entry).

### Added
- **P1.3 goddess-facing UI consolidation** (5 slices, single commit, ref `Docs/plan_post_wave7.md`):
  - **B6** `SubProfileCard`: replaces the inline hero on `/goddess/subs/:subId` with a dedicated component (`client/src/components/goddess/SubProfileCard.tsx`) bundling avatar + identity strip + ownership-status chip. Top-approved-photo placeholder left as a TODO pending a `GET /goddess/subs/{sub_id}/photos` list endpoint.
  - **I4 gauge UI**: new `TributeGauge` component (`client/src/components/goddess/TributeGauge.tsx`) renders a horizontal bar against the existing wave-5 backend (`GET /goddess/subs/{sub_id}/tribute-minimum/gauge`). Mounted in `SubOverviewTab`. Bar fill width injected via the `--gauge-fill-width` CSS variable on a `.gauge-fill-el` class (matches the existing `.planning-tooltip` pattern — no inline styles).
  - **C5 kink overview**: backend `GET /goddess/kinks/overview` returns one row per visible kink item with a `counts` dict mapped over the goddess's subs (single grouped SQL via the new `SubKinkRatingDao.count_ratings_per_item_for_goddess`). Frontend `KinkHeatmap` renders a sticky-header / sticky-first-column heatmap grid with 5-step Tailwind opacity scale (`bg-pink-primary/{10,20,40,60,80}`) at `/goddess/kinks`.
  - **E5 review queue**: backend adds `GET /goddess/review-queue` (cursor-paginated, unified ritual_occurrence + task feed sorted by `submitted_at DESC`) and `POST /goddess/review-queue/bulk` (partial-success bulk approve/reject; per-item domain errors collected into `failed`). Frontend `/goddess/review-queue` route with checkbox selection, sticky `BulkActionBar`, inline reject-reason form (1–500 chars), expandable failure-details panel.
  - **I5 penalty rules UI**: full CRUD page at `/goddess/penalty-rules` on the existing wave-7 backend. `PenaltyRuleForm` reuses one component for create + edit; `fee_amount` field auto-hides unless `action === 'apply_fee'`; `points_delta ≤ 0` enforced via Zod. List with active toggle + two-step delete. Sub override is a UUID text input with a TODO to swap for a sub picker.
  - All 4 new inline query keys migrated to `client/src/lib/queryKeys.ts` factory (`penaltyRules`, `kinkOverview`, `reviewQueue`, `tributeGauge`).
  - `GODDESS_NAV` gains: "Review queue" (after Validations), "Kinks" (after Photo queue), "Penalty rules" (after Rewards & Punishments).

### Added
- **P1.2 sub-facing UI burst** (5 frontend slices, single commit, ref `Docs/plan_post_wave7.md`):
  - **C4** kink matrix: `/profile/kinks` route with category-grouped `KinkMatrix` + per-item `RatingPicker`. Optimistic mutation via `useMutation.onMutate` snapshot/rollback, Zod-parsed `KinkMatrix`/`SubKinkRatingOut` schemas, `needs_confirmation` flag surfaced for safety-flagged items.
  - **D4** limits + safeword: `/profile/limits` route with `LimitForm`/`LimitsList` (hard/soft × low/medium/high) and `TriggerForm`/`TriggersList`. Global `<SafewordBanner />` mounted in `AppLayout` after `<ImpersonationBanner />`, only renders for sub role with `throwOnError: false` so missing safeword does not page the boundary.
  - **E4** today: `/today` route with `RitualCard` (complete/submit + optional evidence file upload) + `TaskCard` + `JournalCTA`. Evidence upload now uses `SubPhotoOut.r2_key` directly (backend schema gained `r2_key: str` in this commit).
  - **F5** journal + merits: sub `/sub/journal` with `JournalEntryForm` (mood + body + optional photo) + paginated `JournalEntryCard` list; goddess `/goddess/subs/:subId/journal` reader with `GoddessCommentForm`; goddess `/goddess/merits` admin route with `RewardTierForm`/`RewardTierList` + `PunishmentTierForm`/`PunishmentTierList` (cost > 0, default_points_penalty ≤ 0 enforced via Zod).
  - **G3** toy inventory: sub `/sub/profile/inventory` with `InventoryGrid` + `ToyForm` (propose-only, hidden until approval); goddess `/goddess/subs/:subId/inventory` mirrors the grid with full CRUD + approve/reject controls.
  - All 14 inline query keys migrated to `client/src/lib/queryKeys.ts` factory under new namespaces (`kinks`, `limits`, `safeword`, `today`, `journal`, `merits`, `toys`).
  - Nav: `SUB_NAV` gains "Today" (first), "Kinks", "Limits", "Journal", "Inventory"; `GODDESS_NAV` gains "Rewards & Punishments". Per-sub goddess journal/inventory routes are deep-linked from `SubManageRoute` (no global nav entry — they require `:subId`).

### Changed
- `SubPhotoOut` schema gains `r2_key: str` so multi-step flows (journal entries, ritual evidence) reference uploads by stable key without parsing presigned URLs. Frontend `r2KeyFromPresignedUrl` workaround removed.

### Removed
- **P0 scope cut** (decision 2026-04-16, ref `Docs/plan_post_wave7.md`): every artefact tied to in-app wishlist + external payment ingestion + YouPay widget deleted. Debt_collector is a manual tracker; money moves outside the app and subs declare each tribute themselves. Specifically:
  - **Wishlist (I1+I2)**: `wishlist_item` table dropped; `wishlist_item.py` model, `wishlist_dao.py`, `wishlist_controller.py`, `routers/wishlist.py`, `schemas/wishlist.py` deleted; wishlist auto-fulfil hook stripped from `payment/controller.py` + `payment/helpers.py`. Routes `/sub/wishlist*` and `/goddess/wishlist*` are gone.
  - **Ingestion infra (A1)**: `payment_webhook_event` table dropped; `payment_webhook_event.py` model, `payment_webhook_dao.py`, `ingest_controller.py`, `services/ingest/` package deleted.
  - **Throne integration (A2)**: `throne_connection` table dropped; `throne_connection.py` model, `throne_dao.py`, `integrations_controller.py`, `routers/goddess_integrations.py`, `schemas/integrations.py` deleted; `THRONE_*` env keys removed (`ROOT_KEK_B64` retained — still used by J4 medical via the envelope crypto service).
  - **YouPay widget (A5)**: `YouPayWidget.tsx`, `services/payments/youpay.ts` deleted; `PaymentFormRoute.tsx` simplified to manual-only; `VITE_YOUPAY_*` env keys removed. "YouPay" remains a valid free-text payment-method name on the manual form.
  - Single migration `328e11d71888_p0_drop_deferred_scope_tables` drops the three tables; `downgrade()` raises `NotImplementedError`. Four PG enum values (`declarationsource.ingested`, `paymentcategory.wishlist`, `allocationtargettype.wishlist_goal`, `notificationtype.wishlist_fulfilled`) remain as orphans because Postgres does not support `ALTER TYPE … DROP VALUE`; flagged in `payment.py`.

### Added
- **Phase-wave-7 backend** (4 phases, 5 new Alembic migrations incl. one merge):
  - **A2** Throne connection: `throne_connection` table (PK=`goddess_id`, FK RESTRICT), stores `access_token_enc BYTEA` encrypted via `encrypt_for_goddess()` with AAD `goddess_id.bytes + b":throne:access_token"`, `access_token_last4` for UI, `dek_version` for future rotation. `GET|POST /goddess/integrations/throne` — POST accepts `{account_id, access_token}`, both verbs return `{is_configured, account_id, token_last4, created_at, updated_at}` with zero plaintext echo. Registered on `server/main.py`. Migration `52252f135ceb`. Required env: `ROOT_KEK_B64` now populated in `server/.env` (32-byte key generated locally).
  - **F4** reward/punishment tiers: `reward_tier(id, goddess_id, name, description, cost INT CHECK>0, active)`, `punishment_tier(…, default_points_penalty INT CHECK<=0)`, `reward_redemption(sub_id, goddess_id, reward_id, cost_snapshot, created_at)` ledger. Goddess CRUD at `GET|POST /goddess/rewards` + `PATCH|DELETE /goddess/rewards/{id}` and the `/goddess/punishments` equivalents. Sub reads active rewards via `GET /sub/rewards`; `POST /sub/rewards/{id}/redeem` re-checks `MeritEventDao.balance_for_sub` (422 if insufficient), inserts redemption row, emits `MeritEvent(source_kind='reward_redeem', source_id=redemption.id, delta=-cost_snapshot)`. `POST /goddess/punishments/{id}/invoke {sub_id}` creates a `Task(status=open)` then emits `MeritEvent(source_kind='punishment_invoke', source_id=task.id, delta=default_points_penalty)`. Both flows protected by the F3 SAVEPOINT-wrapped idempotent insert. `MeritSourceKind` gains `reward_redeem` + `punishment_invoke`. Migration `f4_reward_punishment_tiers`.
  - **I5** penalty_rule engine: `penalty_rule` table (goddess-scoped, optional sub override, `trigger`, `action`, `points_delta`, `fee_amount?`, `cooldown_hours`, `active`) + `penalty_event` log. `PenaltyTrigger(contract_missed|ritual_missed|rolling_late|task_missed)` and `PenaltyAction(notify_only|apply_points|apply_fee)`. `services/penalty/engine.py:apply_penalty()` prefers sub-specific rules, honours cooldown via (rule_id, sub_id, source_kind, source_id) lookup in `penalty_event` for the last `cooldown_hours`, emits the merit event for `apply_points`, and falls back to default_delta when no rule matches. Wired in `cron/rituals.mark_missed_for_today`, `task_controller.reject_task`, `cron_controller` rolling-late + contract-missed branches. `MeritSourceKind` gains `contract_miss` + `rolling_late`. Routes `GET|POST /goddess/penalty-rules` + `PATCH|DELETE /goddess/penalty-rules/{id}`. Migration `i5_penalty_rules`.
  - **J3** consent framework: `consent_text(slug, version, body_md)` + `consent_acceptance(user_id, consent_text_id, accepted_at, ip_address?)` with uniques `(slug, version)` and `(user_id, consent_text_id)`. `services/consent/gate.py:require_consent(slug)` is a FastAPI dependency that raises HTTP 428 with `{error, slug, version, body_md, consent_text_id}` if the user hasn't accepted the latest version. `GET /consent/{slug}`, `POST /consent/{slug}/accept` (201 / 409 on re-accept / 422 on stale id / 404 on unknown), `GET /me/consents`. Seed `seeds/consents.py` populates v1 placeholder markdown for slugs `medical`, `blackmail_upload`, `device_connect` (idempotent; wired into `seeds/bootstrap.py` after `seed_kinks`). Migration `e9578df69b75`.
  - **Merge migration** `bca1c200482d` reunites the F4 + I5 branches that came out of the parallel subagent dispatch (A2 chained linearly through J3 → I5; F4 came off the prior HEAD separately). Verified round-trip `downgrade f4_reward_punishment_tiers` → `upgrade head` clean.

- **Phase-wave-6 backend** (4 phases, 3 new Alembic migrations):
  - **C3** custom kink proposals: sub `POST /sub/profile/kinks/proposals {category_slug, label, notes}` creates a `kink_item(is_custom=true, approved=false, proposed_by_sub_id=…, goddess_id=…)` scoped to the sub's goddess; goddess `GET /goddess/kinks/proposals`, `POST /goddess/kinks/proposals/{id}/approve|reject` (reject hard-deletes). Approved items appear in that goddess's scope only (matrix auto-includes per-goddess rows). No new migration — uses columns from C1.
  - **F3** merit event ledger: `merit_event` table (`sub_id, goddess_id, source_kind, source_id?, delta INT, created_at`) with partial unique index on `(source_kind, source_id) WHERE source_id IS NOT NULL` for cron idempotency. `MeritSourceKind(ritual_complete|ritual_miss|task_complete|task_miss|manual_adjust)`. `services/merits/credits.py` wraps `MeritEventDao.insert_idempotent` with a SAVEPOINT (`begin_nested`) so duplicate-index rollbacks don't kill the outer status-flip transaction. Hooks wired in `task_controller.approve_task/reject_task` + `ritual_controller.approve_occurrence/reject_occurrence` + cron `mark_missed_for_today`. `GET /sub/points-balance` returns `{balance: SUM(delta), last_event_at, event_count}`. Migration `5d8b09d4be39`.
  - **H4** auto_extend renewal flow: H3 review-reminder + H4 auto-extend share a single daily job `contract_09_job` at 09:00 Europe/London. When a contract has `renewal_policy='auto_extend'` and `review_at <= today`, clones the contract into a new `pending_sub_signature` row with `review_at += duration` (monthly via `dateutil.relativedelta`, weekly/biweekly via `timedelta`), copies `clauses_json` + ledger-excluded fields, links via `DebtContractEvent(event_type='contract_renewed', payload={source_contract_id, new_contract_id})`, emits `contract_renewed` notification to both parties. Migration `h4_contract_renewed_enums` adds the enum values via `ALTER TYPE … ADD VALUE IF NOT EXISTS` inside an autocommit_block. Merge migration `d833506ba52b` closes the F3/H4 branch.
  - **D6 + J2** encryption plumbing: `sub_medical` model shell with `dek_version INT default 1` + 5 nullable `BYTEA` columns (`allergies_enc, conditions_enc, medications_enc, blood_type_enc, emergency_contact_enc`) — no read/write API yet (activates in J4). `services/crypto/envelope.py` implements AES-256-GCM with AAD binding (`goddess_id || field_name`), `encrypt_for_goddess()` returns `nonce(12) || ciphertext_and_tag`, `decrypt_for_goddess()` verifies tag + AAD, `rewrap_goddess_dek()` raises `NotImplementedError` (rotation deferred). Tampering any byte raises `InvalidTag`; two calls produce different ciphertexts. Migration `5a94b6fde0ed`.

- **Phase-wave-5 backend** (4 phases, 4 new Alembic migrations):
  - **E3** ritual + task CRUD: goddess `POST|GET /goddess/subs/{sub_id}/rituals`, `PATCH|DELETE /goddess/rituals/{ritual_id}`, `POST /goddess/rituals/occurrences/{id}/approve|reject`. Sub `GET /sub/rituals`, `GET /sub/rituals/today` (Europe/London date), `POST /sub/rituals/occurrences/{id}/complete|submit`. Occurrences gain optional `note` + `evidence_r2_key` columns (B4 stub). New `Task` model with `TaskStatus(open|submitted|approved|rejected|cancelled)` + full goddess/sub CRUD (`POST|GET /goddess/subs/{sub_id}/tasks`, `PATCH|POST /goddess/tasks/{id}(/cancel|/approve|/reject)`, `GET /sub/tasks`, `POST /sub/tasks/{id}/submit`). 409 on illegal status transitions. Migration `6d68f7147e81`.
  - **D5** emergency-stop `POST /sub/panic`: requires `X-Confirm-Password` header re-auth, pauses every non-paused ritual, cancels open + submitted tasks, soft-releases ownership (appends `status_event` with `reason='sub_panic'`), fires high-priority `sub_panic` notification to the goddess. Response returns `{paused_rituals, cancelled_tasks, released, ts}`. Migration `d5a1b2c3e4f5` adds the `sub_panic` NotificationType enum value.
  - **H3** review-reminder cron at 09:00 Europe/London: scans `debt_contract` with `status=active` AND `review_at ∈ [today+14d, today+15d)` (London day boundary), emits one `review_reminder` notification to the goddess per contract with `payload={contract_id, deep_link: "/goddess/contracts/{id}/preview"}`. Idempotent via JSONB `payload->>'contract_id'` lookup. Migration `54e414ed39e5`. Job wired in `services/cron/contracts.py` + `workers/daily_cron.py`.
  - **I4** tribute_minimum + performance gauge (backend): new `tribute_minimum` table (PK=`sub_id`) with `amount NUMERIC(12,2)`, `TributePeriod(weekly|monthly)`, `grace_below_percent NUMERIC(5,4)`. Routes `PUT|GET|DELETE /goddess/subs/{sub_id}/tribute-minimum` and `GET /goddess/subs/{sub_id}/tribute-minimum/gauge`. Gauge aggregates `SUM(PaymentDeclaration.amount)` over the current Europe/London period (weekly Mon-Mon or monthly 1st-1st) and returns `{configured, target_amount, period, actual_this_period, ratio, color, period_start, period_end}` with `color` = red if `ratio < grace_below_percent`, amber if `< 1`, green otherwise. Migration `ebdad219f6a6`.
- **Phase-wave-4 backend** (4 phases, 2 new Alembic migrations):
  - **G2** toy CRUD + approval: sub `POST|GET /sub/profile/toys` (propose, list approved only), goddess `GET|POST /goddess/subs/{sub_id}/toys`, `PATCH|DELETE /goddess/toys/{toy_id}`, `POST /goddess/toys/{toy_id}/approve|reject`. Sub-proposed items stay hidden from the sub until goddess approval; reject hard-deletes. Exposes model fields `name` + `description` directly (not `label`/`notes`).
  - **E2** ritual cron: two APScheduler jobs wired into `workers/daily_cron.py` — 00:00 Europe/London `seed_occurrences_for_today` creates one `ritual_occurrence(status='pending')` per active non-paused ritual (daily; weekly keyed off `ritual.created_at` weekday; custom via `custom_days_bitmask`), 23:59 Europe/London `mark_missed_for_today` flips leftover `pending` rows to `missed`. Idempotent via `ON CONFLICT DO NOTHING` on the `(ritual_id, date)` unique key. New package `server/services/cron/rituals.py`.
  - **H2** contract clauses editor: `PATCH /debts/{contract_id}/clauses` replaces `clauses_json` in bulk (ordered list of `{key,label,body,sort_order}`). When the contract is `active`, writing clauses demotes it to `pending_sub_signature`, clears `signed_at`/`signature_b64`, appends a `DebtContractEvent(event_type='clauses_changed')` and fires a `contract_needs_resignature` notification to the sub. Terminal states (`cancelled`/`settled`/`breached`) return 409. Migration `c4f8a2e91d34` adds the new event type + notification type enum values.
  - **B3** ownership status state machine: `PATCH /goddess/subs/{sub_id}/status` validates transitions against the strict table from spec §16.3 and appends an immutable `status_event(sub_id, goddess_id, from_status, to_status, reason, created_by)` row on success. `GET /goddess/subs/{sub_id}/status-events` returns the audit trail. Illegal transitions respond `422 {"error":"illegal_transition","from":…,"to":…,"allowed":[…]}` via a new `IllegalTransition` exception class wired through `core/exception_handlers.py`. Migration `cfec5074b56b` creates the `status_event` table. Pure transition function lives in `services/profile/status_machine.py`.
- **Phase-wave-3 backend API layer** (20 new routes across 4 phases):
  - **C2** kink matrix API: `GET /sub/profile/kinks` returns the 8-category/44-item matrix grouped by `KinkCategory` with each item's current rating + `needs_confirmation` boolean (`safety_flag AND rating in (curious, loves, fetish_need)`). `PUT /sub/profile/kinks/{item_id}` upserts a `sub_kink_rating`; `GET /goddess/subs/{sub_id}/kinks` exposes the same matrix to the goddess for that sub. Items where `goddess_id IS NULL` are global; per-goddess items are scoped by `user.goddess_id`. Stale ratings outside the current goddess scope are silently excluded from the matrix (history preserved in the row).
  - **D2** limits + triggers CRUD: `GET|POST /sub/profile/limits`, `PATCH|DELETE /sub/profile/limits/{id}` and same 4 verbs for `/sub/profile/triggers`. Sub-initiated edits to a limit clear `acknowledged_by_goddess_at`. `GET /goddess/subs/{sub_id}/limits|triggers` + `POST /goddess/subs/{sub_id}/limits/{limit_id}/acknowledge` (idempotent — already-ack'd returns 200 without re-stamping).
  - **F2** journal API: `POST /sub/journal`, `GET /sub/journal` (cursor pagination via `limit` + `before=ISO-created_at`), `GET /goddess/subs/{sub_id}/journal` (atomic mark-read-and-list in a single DAO method), `PATCH /goddess/journal/{entry_id}/comment` triggers a `journal_comment` notification to the sub. New enum value shipped in migration `9f2b35dbd9ee`.
  - **I2** wishlist CRUD + auto-fulfil: `GET|POST /goddess/wishlist`, `PATCH|DELETE /goddess/wishlist/{id}`, `POST /goddess/wishlist/{id}/approve|reject`. `GET|POST /sub/wishlist` for sub browse + propose. Payment-validation hook in `controllers/payment/controller.py`: when a validated allocation on `PaymentCategory.wishlist` → `AllocationTargetType.wishlist_goal` brings `collected >= target_amount`, flips `status=fulfilled` atomically (idempotent via `status=open` gate) and fires a `wishlist_fulfilled` notification to both parties. New enum value shipped in migration `1f0e764c64a3`.
- **Phase-wave-2 backend foundation** (single Alembic migration `3eebe0a46e45`):
  - **C1** `kink_category` + `kink_item` tables + `sub_kink_rating` with `KinkRating(hard_limit|soft_limit|curious|loves|fetish_need|not_set)` and `uq_sub_kink_rating_sub_item`. `kink_item.slug` is unique per `(slug, goddess_id)` to allow per-goddess custom items. Seed `seeds/kinks.py` populates 8 categories and 44 canonical items, idempotent; wired into `seed_admin_and_goddess()`.
  - **D3** `sub_safeword` table (PK = `sub_id`, FK → `user.id CASCADE`), `SubSafewordIn/Out` schemas, `SubSafewordController`, 3 routes: `POST /sub/profile/safeword` (upsert), `GET /sub/profile/safeword` (sub self), `GET /goddess/subs/{sub_id}/safeword` (goddess view).
  - **F1** `journal_entry` table with `JournalMood(great|good|neutral|low|bad|numb|overwhelmed)`, `body` + optional `photo_r2_key`, goddess-side `read_by_goddess_at` + `goddess_comment`. DAO exposes create + list + mark-read + set-comment; no body-mutation path (immutability guard via omission).
  - **I1** `wishlist_item` table with `WishlistStatus(open|cancelled|fulfilled)`, `WishlistCreatedBy(goddess|sub)`, `target_amount Numeric(12,2)`, per-item `sub_id` for personal gifts. `PaymentCategory.wishlist` + `AllocationTargetType.wishlist_goal` enum extensions. DAO `compute_collected(item_id)` correlates validated allocations via subquery.
  - **Fix** latent `paymentcategory` PG enum bug: `profile_change_fee` value was declared in the Python enum (P1.6) but never added to the PG enum type; migration now `ALTER TYPE … ADD VALUE IF NOT EXISTS 'profile_change_fee'` alongside the new `wishlist` + `wishlist_goal` values.
- **Phase-wave-1 backend foundation** (single Alembic migration `1de5e6292627`):
  - **A1** `payment_webhook_event` table + provider/result enums (`throne|paypal|revolut|cashapp` × `pending|matched|unmatched|duplicate|error`), unique `(provider, provider_event_id)` idempotency key. `services/ingest/matcher.py` resolves sub via 3-priority lookup (`payment_handle` → reference username → reference email); `controllers/ingest_controller.py` wraps it in an idempotent `IngestController.ingest_event()` that rejects non-GBP. `PaymentDeclaration.source` enum extended with `ingested`.
  - **B1** `sub_profile` side table keyed `user_id PK FK→user.id`, fields `real_name|age|gender|pronouns|location|timezone|joined_empire_at|ownership_status`. `Gender(male|female|non_binary|other)` + `OwnershipStatus(free|owned|in_training|collared|blackmailed|released)` enums. `InvitationController.consume()` now creates a default `sub_profile` row in the same transaction as the new `User`.
  - **D1** `sub_limit` + `sub_trigger` tables, `LimitKind(hard|soft)` + `LimitSeverity(low|medium|high)` enums. `acknowledged_by_goddess_at` on limits for sign-off workflow.
  - **E1** `ritual` + `ritual_occurrence` tables. `RitualFrequency(daily|weekly|custom)` with `custom_days_bitmask` + optional `deadline_time`, point economy columns (`points_on_complete|points_on_miss`). `OccurrenceStatus(pending|completed|missed|rejected|submitted)` with unique `(ritual_id, date)` gate for daily cron `bulk_create_for_date`.
  - **G1** `toy_item` table. `ToyCategory(restraint|impact|vibrator|plug|cage|gag|clothing|collar|other)`, `ToyProposedBy(sub|goddess)`, `approved` gate, reserved `lock_code_enc bytea` for phase J.
  - **H1** `debt_contract` gets 3 new columns: `clauses_json JSONB default '[]'::jsonb`, `review_at timestamp?`, `renewal_policy text default 'none'`. `RenewalPolicy(none|reminder|auto_extend)` enum registered. `DebtContractOut` schema unchanged so the UI contract is preserved.
  - **J1** `goddess_kek` table (one DEK per goddess, AES-256-GCM-wrapped by root KEK). `services/crypto/root_kek.py` loads `ROOT_KEK_B64`, generates per-goddess 32-byte DEKs, wraps/unwraps with a 12-byte nonce tagged by `root_kek_version`. `services/crypto/goddess_kek.py:ensure_goddess_kek(session, goddess_id)` idempotent. Bootstrap seeds the goddess KEK when `ROOT_KEK_B64` is configured; gracefully skips otherwise.
- **A5** YouPay widget: `components/payments/YouPayWidget.tsx` renders a deep-link button (default) or iframe (opt-in via `VITE_YOUPAY_IFRAME_ALLOWED="true"`) when a payment method named "YouPay" is selected on `/sub/payments/new`. `services/payments/youpay.ts` builds the `youpay.com.au/pay?amount=&ref=&currency=GBP` URL; `youpay_ref` query param on return auto-populates the reference field.
- Roadmap prep: `cryptography` + `pillow` server deps (Phase J, B4). Env stubs for R2 (sub photos / toys / vault), root KEK + version, Throne webhook / polling. `client/src/assets/goddess/` scaffold folder. `Docs/blackmail_threat_model.md` DRAFT stub (K1 gate).

### Changed
- Roadmap: every `client/src/pages/…` path rewritten to `client/src/routes/…` to match `client/CLAUDE.md` folder convention — no more drift for subagents.
- Replaced 10 generated SVG avatars with 8 AI-illustrated PNG avatars; `AvatarKey.accent_1/accent_2` remain as enum slots but fall back to the default image. Seed data uses only the available art keys.
- Contract preview page: what-if simulator applies the event at the current payment period (not period 1), preserving paid rows, and shows the before/after comparison in a modal with red highlights on changed rows. `ScheduleTable` marks paid periods green with a dot indicator.
- Unified contract totals: `Total interest` and `Total to pay` are now derived from the live simulation (not `min_payment × duration`) and rendered as StatPills alongside `Period rate` / `Monthly rate` on every page using `SimulationChart`. Removed the divergent naive-math tiles from `ContractHeaderSummary`.

### Fixed
- `GET /payments/subs` (goddess sub picker) now returns `first_name`, `last_name`, and `avatar_key`, so goddess-moderated profile changes show immediately in the UI.

### Added
- **P1.1** base64 signature + on-demand PDF: `signature_b64` column replaces `signed_pdf_url`/`signed_pdf_sha256`; `GET /debts/{id}/pdf[?draft=1]` regenerates via WeasyPrint and streams inline; `services/storage/*` removed.
- **P1.3** redesigned contract PDF template: A4 with 8 named sections, full repayment schedule, framed signature block (Europe/London `signed_at`), draft watermark variant.
- **P1.6** avatars + goddess-moderated profile changes: `AvatarKey` enum, 10 seeded SVGs, `payment_handle` on `User` (sub-editable), `ProfileChangeRequest` workflow (approve / reject / set fee — fee paths tied to a special `profile_change_fee` `PaymentDeclaration`), goddess review UI at `/goddess/profile-change-requests`, sub edit UI on `/profile`. `UserOut` now carries `payment_handle`.
- **P1.7** goddess dashboard charts: `GET /goddess/dashboard/charts` returns pre-aggregated monthly revenue, method breakdown, subs-by-status, top-5 subs, 30-day late-rate, and contract-state counts. Client renders with recharts + `ChartPanel`, colours bound to `tokens.css`.
- **P1.8** goddess contract preview page at `/goddess/contracts/:id/preview`: header summary, full schedule table, what-if simulator calling `/debts/simulate`, balance decay chart, DRAFT PDF export button.
- **P0 foundation pass** (audit-driven from Malverse + Calidra):
  - `client/src/utils/env.ts` — only file allowed to read `import.meta.env`; zod schema fails fast on missing/malformed `VITE_*` vars.
  - `client/src/lib/queryKeys.ts` — typed TanStack Query key factory organised by domain; every inline key migrated.
  - `GET /healthz` liveness probe; `openapi_url`/`docs_url`/`redoc_url` forced to `None` in prod.
  - `RequestIdMiddleware` generates/echoes `X-Request-ID`; structlog rewires logging (json in staging/prod, console in dev/test) with `request_id` bound into contextvars per request.
  - Password pepper: `base64(hmac-sha256(PASSWORD_PEPPER, plain))` pre-argon2 hash; dual-verify on login transparently rehashes legacy rows.
  - `RateLimiter` Protocol with `MemoryRateLimiter` / `RedisRateLimiter` impls, toggled by `RATE_LIMITER_BACKEND` + `REDIS_URL`; slowapi route decorators preserved.
  - `make sync-types` + CI `api-types-drift` job fail if `client/src/types/api.generated.ts` drifts from live `/openapi.json`.
  - Makefile `help`, `install`, `quality`, `feed-dbs` composite targets with `## description` annotations.
  - Postgres healthcheck in `docker-compose.yml`.
- `APP_ENV` setting (`dev` | `test` | `staging` | `prod`). Prod auto-enables `Secure` cookies, `SameSite=strict`, and HSTS — no manual toggle.
- Rate limiting (`slowapi`) on `/auth/login`, `/auth/password-reset/request`, `/auth/password-reset/confirm`, `/signup`, and `GET /invite/{token}`.
- `SecurityHeadersMiddleware` — CSP, X-Frame-Options, Referrer-Policy, Permissions-Policy, HSTS (prod only).
- `admin_action` audit log table with DAO + migration; impersonation and admin CRUD mutations are recorded.
- Typed Swagger response schemas for every `/admin/*` entity (`AdminRow*` + generic `AdminListOut`).
- Postgres-backed integration test suite (22 tests) via `testcontainers`: auth flow, rate limit, admin CRUD, impersonation, contract state machine, blacklist cascade.
- `CONTRIBUTING.md`, `CHANGELOG.md`.

### Changed
- Refresh token moved from JSON body to `debt_refresh` HttpOnly cookie on `/auth/login`, `/auth/refresh`, `/auth/logout`. Body fallback retained for legacy clients.
- Admin generic CRUD hardened: per-entity `forbidden_fields` deny-list blocks `id`, `password_hash`, `role`, `goddess_id`, `created_at` from PATCH/POST; `role` changes refused for the `User` entity.
- `debt_controller.py`, `dashboard_controller.py`, `payment_controller.py` split into package folders (`controller.py` + `helpers.py`). Public imports preserved through re-export shims.
- Dev admin/goddess bootstrap credentials moved from `Settings` to constants in `server/seeds/bootstrap.py` — no longer env-provided.
- CORS middleware: `allow_methods` and `allow_headers` tightened from `["*"]` to explicit lists.
- `PASSWORD_RESET_TTL_MINUTES` default aligned to 60 (`Docs/specs.md` §4.2).

### Removed
- Hardcoded `admin_password` / `goddess_password` defaults from `core/config.py` and their values from `server/.env.example` and `README.md`.

### Security
- Argon2id password hashing retained with defaults memory=64MB, time=3, parallelism=4.
- Generic admin CRUD no longer accepts arbitrary role/password_hash/goddess_id changes.
- Refresh tokens are HttpOnly, not accessible from JavaScript, and `Secure` + `SameSite=strict` in prod.

## [0.1.0] — 2026-04-14

First tagged build. Implementation phases 1–10.

### Added
- Phase 1 — Docker infra (Postgres + Mailhog), Make targets, pre-commit, CI green, `make init-dbs` bootstraps admin + goddess + 11 seeded subs in distinct states.
- Phase 2 — Auth: login, logout, refresh, password reset via Resend; argon2id hashing; role/status guards.
- Phase 3 — Invitation flow: create/list/public landing/signup, entry tribute, pending-tribute CTA.
- Phase 4 — Payment methods (drag-reorder, soft-delete), declarations, allocations, validate/reject/recategorise, goddess direct-record.
- Phase 5 — Rolling tributes (`rolling_tribute` table, Europe/London deadline math, `days_late` multiplier, unblock on validation).
- Phase 6 — Debt contracts: tables + enums, AER financial math (`utils/finance.py`), 12-endpoint state machine with one-round counter-proposal rule, per-transition audit rows, live simulation chart.
- Phase 7 — Signature + PDF: WeasyPrint contract template, R2 storage adapter + fake-filesystem fallback, canvas signature, SHA-256 integrity, presigned download URLs (15 min TTL).
- Phase 8 — Ledger + daily cron: `debt_event` ledger, APScheduler @ 08:00 Europe/London, idempotent late detection, blacklist cascade, surprise penalties, mid-contract adjustments (mode-gated), buyout intent flow.
- Phase 9 — Notifications, dashboards, admin: in-process WS publisher with exponential-backoff reconnect, bell + drawer, goddess + sub dashboards with late lists and progress bars, theme toggle, generic `/admin/{entity}` console over 11 entities.
- Phase 10 — Polish + tests retrofit: 21 pytest unit tests, 17 vitest unit tests, UI primitives (`EmptyState`, `ErrorState`, `Skeleton`, `Modal` with focus-trap), empty/error/loading states across all surfaces, mobile-first grids, Playwright visual scaffold, Hetzner deploy playbook.

### Infrastructure
- Pyright strict typecheck replaces mypy.
- `datetime.utcnow()` purged repo-wide in favour of `datetime.now(UTC).replace(tzinfo=None)`.

[Unreleased]: https://github.com/qbecb1zen/Debt_collector/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/qbecb1zen/Debt_collector/releases/tag/v0.1.0
