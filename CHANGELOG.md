# Changelog

All notable changes to this project are documented here. Format follows [Keep a Changelog](https://keepachangelog.com/), the project uses [Semantic Versioning](https://semver.org/), and commits follow [Conventional Commits](https://www.conventionalcommits.org/).

## [Unreleased]

### Removed
- **P0 scope cut** (decision 2026-04-16, ref `Docs/plan_post_wave7.md`): every artefact tied to in-app wishlist + external payment ingestion + YouPay widget deleted. Debt_collector is a manual tracker; money moves outside the app and subs declare each tribute themselves. Specifically:
  - **Wishlist (I1+I2)**: `wishlist_item` table dropped; `wishlist_item.py` model, `wishlist_dao.py`, `wishlist_controller.py`, `routers/wishlist.py`, `schemas/wishlist.py` deleted; wishlist auto-fulfil hook stripped from `payment/controller.py` + `payment/helpers.py`. Routes `/sub/wishlist*` and `/goddess/wishlist*` are gone.
  - **Ingestion infra (A1)**: `payment_webhook_event` table dropped; `payment_webhook_event.py` model, `payment_webhook_dao.py`, `ingest_controller.py`, `services/ingest/` package deleted.
  - **Throne integration (A2)**: `throne_connection` table dropped; `throne_connection.py` model, `throne_dao.py`, `integrations_controller.py`, `routers/goddess_integrations.py`, `schemas/integrations.py` deleted; `THRONE_*` env keys removed (`ROOT_KEK_B64` retained — still used by J4 medical via the envelope crypto service).
  - **YouPay widget (A5)**: `YouPayWidget.tsx`, `services/payments/youpay.ts` deleted; `PaymentFormRoute.tsx` simplified to manual-only; `VITE_YOUPAY_*` env keys removed. "YouPay" remains a valid free-text payment-method name on the manual form.
  - Single migration `328e11d71888_p0_drop_deferred_scope_tables` drops the three tables; `downgrade()` raises `NotImplementedError`. Four PG enum values (`declarationsource.ingested`, `paymentcategory.wishlist`, `allocationtargettype.wishlist_goal`, `notificationtype.wishlist_fulfilled`) remain as orphans because Postgres does not support `ALTER TYPE … DROP VALUE`; flagged in `payment.py`.

### Added
- **Phase-wave-7 backend** (4 phases, 5 new Alembic migrations incl. one merge):
  - **A2** Throne connection: `throne_connection` table (PK=`goddess_id`, FK RESTRICT), stores `access_token_enc BYTEA` encrypted via `encrypt_for_goddess()` with AAD `goddess_id.bytes + b":throne:access_token"`, `access_token_last4` for UI, `dek_version` for future rotation. `GET|POST /goddess/integrations/throne` — POST accepts `{account_id, access_token}`, both verbs return `{is_configured, account_id, token_last4, created_at, updated_at}` with zero plaintext echo. Registered on `server/main.py`. Migration `52252f135ceb`. Required env: `ROOT_KEK_B64` now populated in `server/.env` (32-byte key generated locally).
  - **F4** reward/punishment tiers: `reward_tier(id, goddess_id, name, description, cost INT CHECK>0, active)`, `punishment_tier(…, default_points_penalty INT CHECK<=0)`, `reward_redemption(sub_id, goddess_id, reward_id, cost_snapshot, created_at)` ledger. Goddess CRUD at `GET|POST /goddess/rewards` + `PATCH|DELETE /goddess/rewards/{id}` and the `/goddess/punishments` equivalents. Sub reads active rewards via `GET /sub/rewards`; `POST /sub/rewards/{id}/redeem` re-checks `MeritEventDao.balance_for_sub` (422 if insufficient), inserts redemption row, emits `MeritEvent(source_kind='reward_redeem', source_id=redemption.id, delta=-cost_snapshot)`. `POST /goddess/punishments/{id}/invoke {sub_id}` creates a `Task(status=open)` then emits `MeritEvent(source_kind='punishment_invoke', source_id=task.id, delta=default_points_penalty)`. Both flows protected by the F3 SAVEPOINT-wrapped idempotent insert. `MeritSourceKind` gains `reward_redeem` + `punishment_invoke`. Migration `f4_reward_punishment_tiers`.
  - **I5** penalty_rule engine: `penalty_rule` table (goddess-scoped, optional sub override, `trigger`, `action`, `points_delta`, `fee_amount?`, `cooldown_hours`, `active`) + `penalty_event` log. `PenaltyTrigger(contract_missed|ritual_missed|rolling_late|task_missed)` and `PenaltyAction(notify_only|apply_points|apply_fee)`. `services/penalty/engine.py:apply_penalty()` prefers sub-specific rules, honours cooldown via (rule_id, sub_id, source_kind, source_id) lookup in `penalty_event` for the last `cooldown_hours`, emits the merit event for `apply_points`, and falls back to default_delta when no rule matches. Wired in `cron/rituals.mark_missed_for_today`, `task_controller.reject_task`, `cron_controller` rolling-late + contract-missed branches. `MeritSourceKind` gains `contract_miss` + `rolling_late`. Routes `GET|POST /goddess/penalty-rules` + `PATCH|DELETE /goddess/penalty-rules/{id}`. Migration `i5_penalty_rules`.
  - **J3** consent framework: `consent_text(slug, version, body_md)` + `consent_acceptance(user_id, consent_text_id, accepted_at, ip_address?)` with uniques `(slug, version)` and `(user_id, consent_text_id)`. `services/consent/gate.py:require_consent(slug)` is a FastAPI dependency that raises HTTP 428 with `{error, slug, version, body_md, consent_text_id}` if the user hasn't accepted the latest version. `GET /consent/{slug}`, `POST /consent/{slug}/accept` (201 / 409 on re-accept / 422 on stale id / 404 on unknown), `GET /me/consents`. Seed `seeds/consents.py` populates v1 placeholder markdown for slugs `medical`, `blackmail_upload`, `device_connect` (idempotent; wired into `seeds/bootstrap.py` after `seed_kinks`). Migration `e9578df69b75`.
  - **Merge migration** `bca1c200482d` reunites the F4 + I5 branches that came out of the parallel subagent dispatch (A2 chained linearly through J3 → I5; F4 came off the prior HEAD separately). Verified round-trip `downgrade f4_reward_punishment_tiers` → `upgrade head` clean.

- **Phase-wave-6 backend** (4 phases, 3 new Alembic migrations):
  - **C3** custom kink proposals: sub `POST /sub/profile/kinks/proposals {category_slug, label, notes}` creates a `kink_item(is_custom=true, approved=false, proposed_by_sub_id=…, goddess_id=…)` scoped to the sub's goddess; goddess `GET /goddess/kinks/proposals`, `POST /goddess/kinks/proposals/{id}/approve|reject` (reject hard-deletes). Approved items appear in that goddess's scope only (matrix auto-includes per-goddess rows). No new migration — uses columns from C1.
  - **F3** merit event ledger: `merit_event` table (`sub_id, goddess_id, source_kind, source_id?, delta INT, created_at`) with partial unique index on `(source_kind, source_id) WHERE source_id IS NOT NULL` for cron idempotency. `MeritSourceKind(ritual_complete|ritual_miss|task_complete|task_miss|manual_adjust)`. `services/merits/credits.py` wraps `MeritEventDao.insert_idempotent` with a SAVEPOINT (`begin_nested`) so duplicate-index rollbacks don't kill the outer status-flip transaction. Hooks wired in `task_controller.approve_task/reject_task` + `ritual_controller.approve_occurrence/reject_occurrence` + cron `mark_missed_for_today`. `GET /sub/points-balance` returns `{balance: SUM(delta), last_event_at, event_count}`. Migration `5d8b09d4be39`.
  - **H4** auto_extend renewal flow: H3 review-reminder + H4 auto-extend share a single daily job `contract_09_job` at 09:00 Europe/London. When a contract has `renewal_policy='auto_extend'` and `review_at <= today`, clones the contract into a new `pending_sub_signature` row with `review_at += duration` (monthly via `dateutil.relativedelta`, weekly/biweekly via `timedelta`), copies `clauses_json` + ledger-excluded fields, links via `DebtContractEvent(event_type='contract_renewed', payload={source_contract_id, new_contract_id})`, emits `contract_renewed` notification to both parties. Migration `h4_contract_renewed_enums` adds the enum values via `ALTER TYPE … ADD VALUE IF NOT EXISTS` inside an autocommit_block. Merge migration `d833506ba52b` closes the F3/H4 branch.
  - **D6 + J2** encryption plumbing: `sub_medical` model shell with `dek_version INT default 1` + 5 nullable `BYTEA` columns (`allergies_enc, conditions_enc, medications_enc, blood_type_enc, emergency_contact_enc`) — no read/write API yet (activates in J4). `services/crypto/envelope.py` implements AES-256-GCM with AAD binding (`goddess_id || field_name`), `encrypt_for_goddess()` returns `nonce(12) || ciphertext_and_tag`, `decrypt_for_goddess()` verifies tag + AAD, `rewrap_goddess_dek()` raises `NotImplementedError` (rotation deferred). Tampering any byte raises `InvalidTag`; two calls produce different ciphertexts. Migration `5a94b6fde0ed`.

- **Phase-wave-5 backend** (4 phases, 4 new Alembic migrations):
  - **E3** ritual + task CRUD: goddess `POST|GET /goddess/subs/{sub_id}/rituals`, `PATCH|DELETE /goddess/rituals/{ritual_id}`, `POST /goddess/rituals/occurrences/{id}/approve|reject`. Sub `GET /sub/rituals`, `GET /sub/rituals/today` (Europe/London date), `POST /sub/rituals/occurrences/{id}/complete|submit`. Occurrences gain optional `note` + `evidence_r2_key` columns (B4 stub). New `Task` model with `TaskStatus(open|submitted|approved|rejected|cancelled)` + full goddess/sub CRUD (`POST|GET /goddess/subs/{sub_id}/tasks`, `PATCH|POST /goddess/tasks/{id}(/cancel|/approve|/reject)`, `GET /sub/tasks`, `POST /sub/tasks/{id}/submit`). 409 on illegal status transitions. Migration `6d68f7147e81`.
  - **D5** emergency-stop `POST /sub/panic`: requires `X-Confirm-Password` header re-auth, pauses every non-paused ritual, cancels open + submitted tasks, soft-releases ownership (appends `status_event` with `reason='sub_panic'`), fires high-priority `sub_panic` notification to the goddess. Response returns `{paused_rituals, cancelled_tasks, released, ts}`. Migration `d5a1b2c3e4f5` adds the `sub_panic` NotificationType enum value.
  - **H3** review-reminder cron at 09:00 Europe/London: scans `debt_contract` with `status=active` AND `review_at ∈ [today+14d, today+15d)` (London day boundary), emits one `review_reminder` notification to the goddess per contract with `payload={contract_id, deep_link: "/goddess/contracts/{id}/preview"}`. Idempotent via JSONB `payload->>'contract_id'` lookup. Migration `54e414ed39e5`. Job wired in `services/cron/contracts.py` + `workers/daily_cron.py`.
  - **I4** tribute_minimum + performance gauge (backend): new `tribute_minimum` table (PK=`sub_id`) with `amount NUMERIC(12,2)`, `TributePeriod(weekly|monthly)`, `grace_below_percent NUMERIC(5,4)`. Routes `PUT|GET|DELETE /goddess/subs/{sub_id}/tribute-minimum` and `GET /goddess/subs/{sub_id}/tribute-minimum/gauge`. Gauge aggregates `SUM(PaymentDeclaration.amount)` over the current Europe/London period (weekly Mon-Mon or monthly 1st-1st) and returns `{configured, target_amount, period, actual_this_period, ratio, color, period_start, period_end}` with `color` = red if `ratio < grace_below_percent`, amber if `< 1`, green otherwise. Migration `ebdad219f6a6`.
- **Phase-wave-4 backend** (4 phases, 2 new Alembic migrations):
  - **G2** toy CRUD + approval: sub `POST|GET /sub/profile/toys` (propose, list approved only), goddess `GET|POST /goddess/subs/{sub_id}/toys`, `PATCH|DELETE /goddess/toys/{toy_id}`, `POST /goddess/toys/{toy_id}/approve|reject`. Sub-proposed items stay hidden from the sub until goddess approval; reject hard-deletes. Exposes model fields `name` + `description` directly (not `label`/`notes`).
  - **E2** ritual cron: two APScheduler jobs wired into `workers/daily_cron.py` — 00:00 Europe/London `seed_occurrences_for_today` creates one `ritual_occurrence(status='pending')` per active non-paused ritual (daily; weekly keyed off `ritual.created_at` weekday; custom via `custom_days_bitmask`), 23:59 Europe/London `mark_missed_for_today` flips leftover `pending` rows to `missed`. Idempotent via `ON CONFLICT DO NOTHING` on the `(ritual_id, date)` unique key. New package `server/services/cron/rituals.py`.
  - **H2** contract clauses editor: `PATCH /debts/{contract_id}/clauses` replaces `clauses_json` in bulk (ordered list of `{key,label,body,sort_order}`). When the contract is `active`, writing clauses demotes it to `pending_sub_signature`, clears `signed_at`/`signature_b64`, appends a `DebtContractEvent(event_type='clauses_changed')` and fires a `contract_needs_resignature` notification to the sub. Terminal states (`cancelled`/`settled`/`breached`) return 409. Migration `c4f8a2e91d34` adds the new event type + notification type enum values.
  - **B3** ownership status state machine: `PATCH /goddess/subs/{sub_id}/status` validates transitions against the strict table from spec §16.3 and appends an immutable `status_event(sub_id, goddess_id, from_status, to_status, reason, created_by)` row on success. `GET /goddess/subs/{sub_id}/status-events` returns the audit trail. Illegal transitions respond `422 {"error":"illegal_transition","from":…,"to":…,"allowed":[…]}` via a new `IllegalTransition` exception class wired through `core/exception_handlers.py`. Migration `cfec5074b56b` creates the `status_event` table. Pure transition function lives in `services/profile/status_machine.py`.
- **Phase-wave-3 backend API layer** (20 new routes across 4 phases):
  - **C2** kink matrix API: `GET /sub/profile/kinks` returns the 8-category/44-item matrix grouped by `KinkCategory` with each item's current rating + `needs_confirmation` boolean (`safety_flag AND rating in (curious, loves, fetish_need)`). `PUT /sub/profile/kinks/{item_id}` upserts a `sub_kink_rating`; `GET /goddess/subs/{sub_id}/kinks` exposes the same matrix to the goddess for that sub. Items where `goddess_id IS NULL` are global; per-goddess items are scoped by `user.goddess_id`. Stale ratings outside the current goddess scope are silently excluded from the matrix (history preserved in the row).
  - **D2** limits + triggers CRUD: `GET|POST /sub/profile/limits`, `PATCH|DELETE /sub/profile/limits/{id}` and same 4 verbs for `/sub/profile/triggers`. Sub-initiated edits to a limit clear `acknowledged_by_goddess_at`. `GET /goddess/subs/{sub_id}/limits|triggers` + `POST /goddess/subs/{sub_id}/limits/{limit_id}/acknowledge` (idempotent — already-ack'd returns 200 without re-stamping).
  - **F2** journal API: `POST /sub/journal`, `GET /sub/journal` (cursor pagination via `limit` + `before=ISO-created_at`), `GET /goddess/subs/{sub_id}/journal` (atomic mark-read-and-list in a single DAO method), `PATCH /goddess/journal/{entry_id}/comment` triggers a `journal_comment` notification to the sub. New enum value shipped in migration `9f2b35dbd9ee`.
  - **I2** wishlist CRUD + auto-fulfil: `GET|POST /goddess/wishlist`, `PATCH|DELETE /goddess/wishlist/{id}`, `POST /goddess/wishlist/{id}/approve|reject`. `GET|POST /sub/wishlist` for sub browse + propose. Payment-validation hook in `controllers/payment/controller.py`: when a validated allocation on `PaymentCategory.wishlist` → `AllocationTargetType.wishlist_goal` brings `collected >= target_amount`, flips `status=fulfilled` atomically (idempotent via `status=open` gate) and fires a `wishlist_fulfilled` notification to both parties. New enum value shipped in migration `1f0e764c64a3`.
- **Phase-wave-2 backend foundation** (single Alembic migration `3eebe0a46e45`):
  - **C1** `kink_category` + `kink_item` tables + `sub_kink_rating` with `KinkRating(hard_limit|soft_limit|curious|loves|fetish_need|not_set)` and `uq_sub_kink_rating_sub_item`. `kink_item.slug` is unique per `(slug, goddess_id)` to allow per-goddess custom items. Seed `seeds/kinks.py` populates 8 categories and 44 canonical items, idempotent; wired into `seed_admin_and_goddess()`.
  - **D3** `sub_safeword` table (PK = `sub_id`, FK → `user.id CASCADE`), `SubSafewordIn/Out` schemas, `SubSafewordController`, 3 routes: `POST /sub/profile/safeword` (upsert), `GET /sub/profile/safeword` (sub self), `GET /goddess/subs/{sub_id}/safeword` (goddess view).
  - **F1** `journal_entry` table with `JournalMood(great|good|neutral|low|bad|numb|overwhelmed)`, `body` + optional `photo_r2_key`, goddess-side `read_by_goddess_at` + `goddess_comment`. DAO exposes create + list + mark-read + set-comment; no body-mutation path (immutability guard via omission).
  - **I1** `wishlist_item` table with `WishlistStatus(open|cancelled|fulfilled)`, `WishlistCreatedBy(goddess|sub)`, `target_amount Numeric(12,2)`, per-item `sub_id` for personal gifts. `PaymentCategory.wishlist` + `AllocationTargetType.wishlist_goal` enum extensions. DAO `compute_collected(item_id)` correlates validated allocations via subquery.
  - **Fix** latent `paymentcategory` PG enum bug: `profile_change_fee` value was declared in the Python enum (P1.6) but never added to the PG enum type; migration now `ALTER TYPE … ADD VALUE IF NOT EXISTS 'profile_change_fee'` alongside the new `wishlist` + `wishlist_goal` values.
- **Phase-wave-1 backend foundation** (single Alembic migration `1de5e6292627`):
  - **A1** `payment_webhook_event` table + provider/result enums (`throne|paypal|revolut|cashapp` × `pending|matched|unmatched|duplicate|error`), unique `(provider, provider_event_id)` idempotency key. `services/ingest/matcher.py` resolves sub via 3-priority lookup (`payment_handle` → reference username → reference email); `controllers/ingest_controller.py` wraps it in an idempotent `IngestController.ingest_event()` that rejects non-GBP. `PaymentDeclaration.source` enum extended with `ingested`.
  - **B1** `sub_profile` side table keyed `user_id PK FK→user.id`, fields `real_name|age|gender|pronouns|location|timezone|joined_empire_at|ownership_status`. `Gender(male|female|non_binary|other)` + `OwnershipStatus(free|owned|in_training|collared|blackmailed|released)` enums. `InvitationController.consume()` now creates a default `sub_profile` row in the same transaction as the new `User`.
  - **D1** `sub_limit` + `sub_trigger` tables, `LimitKind(hard|soft)` + `LimitSeverity(low|medium|high)` enums. `acknowledged_by_goddess_at` on limits for sign-off workflow.
  - **E1** `ritual` + `ritual_occurrence` tables. `RitualFrequency(daily|weekly|custom)` with `custom_days_bitmask` + optional `deadline_time`, point economy columns (`points_on_complete|points_on_miss`). `OccurrenceStatus(pending|completed|missed|rejected|submitted)` with unique `(ritual_id, date)` gate for daily cron `bulk_create_for_date`.
  - **G1** `toy_item` table. `ToyCategory(restraint|impact|vibrator|plug|cage|gag|clothing|collar|other)`, `ToyProposedBy(sub|goddess)`, `approved` gate, reserved `lock_code_enc bytea` for phase J.
  - **H1** `debt_contract` gets 3 new columns: `clauses_json JSONB default '[]'::jsonb`, `review_at timestamp?`, `renewal_policy text default 'none'`. `RenewalPolicy(none|reminder|auto_extend)` enum registered. `DebtContractOut` schema unchanged so the UI contract is preserved.
  - **J1** `goddess_kek` table (one DEK per goddess, AES-256-GCM-wrapped by root KEK). `services/crypto/root_kek.py` loads `ROOT_KEK_B64`, generates per-goddess 32-byte DEKs, wraps/unwraps with a 12-byte nonce tagged by `root_kek_version`. `services/crypto/goddess_kek.py:ensure_goddess_kek(session, goddess_id)` idempotent. Bootstrap seeds the goddess KEK when `ROOT_KEK_B64` is configured; gracefully skips otherwise.
- **A5** YouPay widget: `components/payments/YouPayWidget.tsx` renders a deep-link button (default) or iframe (opt-in via `VITE_YOUPAY_IFRAME_ALLOWED="true"`) when a payment method named "YouPay" is selected on `/sub/payments/new`. `services/payments/youpay.ts` builds the `youpay.com.au/pay?amount=&ref=&currency=GBP` URL; `youpay_ref` query param on return auto-populates the reference field.
- Roadmap prep: `cryptography` + `pillow` server deps (Phase J, B4). Env stubs for R2 (sub photos / toys / vault), root KEK + version, Throne webhook / polling. `client/src/assets/goddess/` scaffold folder. `Docs/blackmail_threat_model.md` DRAFT stub (K1 gate).

### Changed
- Roadmap: every `client/src/pages/…` path rewritten to `client/src/routes/…` to match `client/CLAUDE.md` folder convention — no more drift for subagents.
- Replaced 10 generated SVG avatars with 8 AI-illustrated PNG avatars; `AvatarKey.accent_1/accent_2` remain as enum slots but fall back to the default image. Seed data uses only the available art keys.
- Contract preview page: what-if simulator applies the event at the current payment period (not period 1), preserving paid rows, and shows the before/after comparison in a modal with red highlights on changed rows. `ScheduleTable` marks paid periods green with a dot indicator.
- Unified contract totals: `Total interest` and `Total to pay` are now derived from the live simulation (not `min_payment × duration`) and rendered as StatPills alongside `Period rate` / `Monthly rate` on every page using `SimulationChart`. Removed the divergent naive-math tiles from `ContractHeaderSummary`.

### Fixed
- `GET /payments/subs` (goddess sub picker) now returns `first_name`, `last_name`, and `avatar_key`, so goddess-moderated profile changes show immediately in the UI.

### Added
- **P1.1** base64 signature + on-demand PDF: `signature_b64` column replaces `signed_pdf_url`/`signed_pdf_sha256`; `GET /debts/{id}/pdf[?draft=1]` regenerates via WeasyPrint and streams inline; `services/storage/*` removed.
- **P1.3** redesigned contract PDF template: A4 with 8 named sections, full repayment schedule, framed signature block (Europe/London `signed_at`), draft watermark variant.
- **P1.6** avatars + goddess-moderated profile changes: `AvatarKey` enum, 10 seeded SVGs, `payment_handle` on `User` (sub-editable), `ProfileChangeRequest` workflow (approve / reject / set fee — fee paths tied to a special `profile_change_fee` `PaymentDeclaration`), goddess review UI at `/goddess/profile-change-requests`, sub edit UI on `/profile`. `UserOut` now carries `payment_handle`.
- **P1.7** goddess dashboard charts: `GET /goddess/dashboard/charts` returns pre-aggregated monthly revenue, method breakdown, subs-by-status, top-5 subs, 30-day late-rate, and contract-state counts. Client renders with recharts + `ChartPanel`, colours bound to `tokens.css`.
- **P1.8** goddess contract preview page at `/goddess/contracts/:id/preview`: header summary, full schedule table, what-if simulator calling `/debts/simulate`, balance decay chart, DRAFT PDF export button.
- **P0 foundation pass** (audit-driven from Malverse + Calidra):
  - `client/src/utils/env.ts` — only file allowed to read `import.meta.env`; zod schema fails fast on missing/malformed `VITE_*` vars.
  - `client/src/lib/queryKeys.ts` — typed TanStack Query key factory organised by domain; every inline key migrated.
  - `GET /healthz` liveness probe; `openapi_url`/`docs_url`/`redoc_url` forced to `None` in prod.
  - `RequestIdMiddleware` generates/echoes `X-Request-ID`; structlog rewires logging (json in staging/prod, console in dev/test) with `request_id` bound into contextvars per request.
  - Password pepper: `base64(hmac-sha256(PASSWORD_PEPPER, plain))` pre-argon2 hash; dual-verify on login transparently rehashes legacy rows.
  - `RateLimiter` Protocol with `MemoryRateLimiter` / `RedisRateLimiter` impls, toggled by `RATE_LIMITER_BACKEND` + `REDIS_URL`; slowapi route decorators preserved.
  - `make sync-types` + CI `api-types-drift` job fail if `client/src/types/api.generated.ts` drifts from live `/openapi.json`.
  - Makefile `help`, `install`, `quality`, `feed-dbs` composite targets with `## description` annotations.
  - Postgres healthcheck in `docker-compose.yml`.
- `APP_ENV` setting (`dev` | `test` | `staging` | `prod`). Prod auto-enables `Secure` cookies, `SameSite=strict`, and HSTS — no manual toggle.
- Rate limiting (`slowapi`) on `/auth/login`, `/auth/password-reset/request`, `/auth/password-reset/confirm`, `/signup`, and `GET /invite/{token}`.
- `SecurityHeadersMiddleware` — CSP, X-Frame-Options, Referrer-Policy, Permissions-Policy, HSTS (prod only).
- `admin_action` audit log table with DAO + migration; impersonation and admin CRUD mutations are recorded.
- Typed Swagger response schemas for every `/admin/*` entity (`AdminRow*` + generic `AdminListOut`).
- Postgres-backed integration test suite (22 tests) via `testcontainers`: auth flow, rate limit, admin CRUD, impersonation, contract state machine, blacklist cascade.
- `CONTRIBUTING.md`, `CHANGELOG.md`.

### Changed
- Refresh token moved from JSON body to `debt_refresh` HttpOnly cookie on `/auth/login`, `/auth/refresh`, `/auth/logout`. Body fallback retained for legacy clients.
- Admin generic CRUD hardened: per-entity `forbidden_fields` deny-list blocks `id`, `password_hash`, `role`, `goddess_id`, `created_at` from PATCH/POST; `role` changes refused for the `User` entity.
- `debt_controller.py`, `dashboard_controller.py`, `payment_controller.py` split into package folders (`controller.py` + `helpers.py`). Public imports preserved through re-export shims.
- Dev admin/goddess bootstrap credentials moved from `Settings` to constants in `server/seeds/bootstrap.py` — no longer env-provided.
- CORS middleware: `allow_methods` and `allow_headers` tightened from `["*"]` to explicit lists.
- `PASSWORD_RESET_TTL_MINUTES` default aligned to 60 (`Docs/specs.md` §4.2).

### Removed
- Hardcoded `admin_password` / `goddess_password` defaults from `core/config.py` and their values from `server/.env.example` and `README.md`.

### Security
- Argon2id password hashing retained with defaults memory=64MB, time=3, parallelism=4.
- Generic admin CRUD no longer accepts arbitrary role/password_hash/goddess_id changes.
- Refresh tokens are HttpOnly, not accessible from JavaScript, and `Secure` + `SameSite=strict` in prod.

## [0.1.0] — 2026-04-14

First tagged build. Implementation phases 1–10.

### Added
- Phase 1 — Docker infra (Postgres + Mailhog), Make targets, pre-commit, CI green, `make init-dbs` bootstraps admin + goddess + 11 seeded subs in distinct states.
- Phase 2 — Auth: login, logout, refresh, password reset via Resend; argon2id hashing; role/status guards.
- Phase 3 — Invitation flow: create/list/public landing/signup, entry tribute, pending-tribute CTA.
- Phase 4 — Payment methods (drag-reorder, soft-delete), declarations, allocations, validate/reject/recategorise, goddess direct-record.
- Phase 5 — Rolling tributes (`rolling_tribute` table, Europe/London deadline math, `days_late` multiplier, unblock on validation).
- Phase 6 — Debt contracts: tables + enums, AER financial math (`utils/finance.py`), 12-endpoint state machine with one-round counter-proposal rule, per-transition audit rows, live simulation chart.
- Phase 7 — Signature + PDF: WeasyPrint contract template, R2 storage adapter + fake-filesystem fallback, canvas signature, SHA-256 integrity, presigned download URLs (15 min TTL).
- Phase 8 — Ledger + daily cron: `debt_event` ledger, APScheduler @ 08:00 Europe/London, idempotent late detection, blacklist cascade, surprise penalties, mid-contract adjustments (mode-gated), buyout intent flow.
- Phase 9 — Notifications, dashboards, admin: in-process WS publisher with exponential-backoff reconnect, bell + drawer, goddess + sub dashboards with late lists and progress bars, theme toggle, generic `/admin/{entity}` console over 11 entities.
- Phase 10 — Polish + tests retrofit: 21 pytest unit tests, 17 vitest unit tests, UI primitives (`EmptyState`, `ErrorState`, `Skeleton`, `Modal` with focus-trap), empty/error/loading states across all surfaces, mobile-first grids, Playwright visual scaffold, Hetzner deploy playbook.

### Infrastructure
- Pyright strict typecheck replaces mypy.
- `datetime.utcnow()` purged repo-wide in favour of `datetime.now(UTC).replace(tzinfo=None)`.

[Unreleased]: https://github.com/qbecb1zen/Debt_collector/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/qbecb1zen/Debt_collector/releases/tag/v0.1.0
